---
phase: 07-html-first-ai-generation-and-lovable-style-editor
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(public)/[username]/[slug]/page.tsx
  - src/app/(public)/[username]/[slug]/opengraph-image.tsx
  - src/app/(public)/[username]/[slug]/route.ts
  - src/app/page.tsx
  - src/app/(dashboard)/dashboard/dashboard-nav.tsx
autonomous: true
requirements: [P7-04, P7-07, P7-08]
must_haves:
  truths:
    - "Public route serves raw HTML for published websites"
    - "Draft websites return 404 on public URL"
    - "Archived websites show Vietnamese archived message"
    - "Landing page shows hero, features, how-it-works, footer in Vietnamese"
    - "Dashboard nav shows AppGen brand"
  artifacts:
    - path: "src/app/(public)/[username]/[slug]/route.ts"
      provides: "Raw HTML route handler"
      exports: ["GET"]
    - path: "src/app/page.tsx"
      provides: "Vietnamese landing page"
      contains: "Bắt đầu miễn phí"
    - path: "src/app/(dashboard)/dashboard/dashboard-nav.tsx"
      provides: "Rebranded nav"
      contains: "AppGen"
  key_links:
    - from: "src/app/(public)/[username]/[slug]/route.ts"
      to: "src/db/schema.ts"
      via: "db.select().from(websites)"
      pattern: "htmlContent"
---

<objective>
Public route handler (raw HTML), landing page rewrite, and dashboard rebrand.

Purpose: Public-facing pages and branding updated for the HTML-first model.
Output: route.ts replacing page.tsx, landing page, AppGen brand.
</objective>

<execution_context>
@D:/STEVE/steve/.claude/get-shit-done/workflows/execute-plan.md
@D:/STEVE/steve/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-html-first-ai-generation-and-lovable-style-editor/07-CONTEXT.md
@.planning/phases/07-html-first-ai-generation-and-lovable-style-editor/07-RESEARCH.md
@.planning/phases/07-html-first-ai-generation-and-lovable-style-editor/07-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Public route handler — replace page.tsx with route.ts</name>
  <files>src/app/(public)/[username]/[slug]/route.ts, src/app/(public)/[username]/[slug]/page.tsx, src/app/(public)/[username]/[slug]/opengraph-image.tsx</files>
  <read_first>
    - src/app/(public)/[username]/[slug]/page.tsx (file to delete — understand current structure)
    - src/app/(public)/[username]/[slug]/opengraph-image.tsx (file to delete)
    - src/db/schema.ts (for websites and profiles table references)
  </read_first>
  <action>
**CRITICAL ORDER: Delete page.tsx and opengraph-image.tsx BEFORE creating route.ts.** Next.js cannot have both page.tsx and route.ts in the same segment.

**Step 1:** Delete these two files:
- `src/app/(public)/[username]/[slug]/page.tsx`
- `src/app/(public)/[username]/[slug]/opengraph-image.tsx`

**Step 2:** Create `src/app/(public)/[username]/[slug]/route.ts`:

```typescript
import { db } from "@/db";
import { websites, profiles } from "@/db/schema";
import { and, eq } from "drizzle-orm";

export async function GET(
  _request: Request,
  { params }: { params: Promise<{ username: string; slug: string }> }
) {
  const { username, slug } = await params;

  // Look up profile by username
  const profileResults = await db.select().from(profiles)
    .where(eq(profiles.username, username)).limit(1);
  if (profileResults.length === 0) {
    return new Response(null, { status: 404 });
  }

  // Look up website by userId + slug
  const websiteResults = await db.select().from(websites)
    .where(and(eq(websites.userId, profileResults[0].id), eq(websites.slug, slug)))
    .limit(1);
  if (websiteResults.length === 0) {
    return new Response(null, { status: 404 });
  }

  const website = websiteResults[0];

  // Draft → 404
  if (website.status === "draft") {
    return new Response(null, { status: 404 });
  }

  // Archived → plain HTML message
  if (website.status === "archived") {
    return new Response(
      `<!DOCTYPE html><html lang="vi"><head><meta charset="utf-8"><title>Trang đã bị lưu trữ</title></head><body style="display:flex;align-items:center;justify-content:center;min-height:100vh;font-family:sans-serif;"><p>Trang đã bị lưu trữ</p></body></html>`,
      { status: 200, headers: { "Content-Type": "text/html; charset=utf-8" } }
    );
  }

  // Published — serve raw HTML
  const htmlContent = website.htmlContent as string | null;
  if (!htmlContent) {
    return new Response(null, { status: 404 });
  }

  return new Response(htmlContent, {
    status: 200,
    headers: { "Content-Type": "text/html; charset=utf-8" },
  });
}
```

Key: `params` is `Promise` in Next.js 16 — must `await params`. No `page.tsx` or `opengraph-image.tsx` siblings.
  </action>
  <verify>
    <automated>npm run typecheck && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - File does NOT exist: src/app/(public)/[username]/[slug]/page.tsx
    - File does NOT exist: src/app/(public)/[username]/[slug]/opengraph-image.tsx
    - File exists: src/app/(public)/[username]/[slug]/route.ts
    - grep "export async function GET" src/app/(public)/[username]/[slug]/route.ts returns a match
    - grep "Content-Type.*text/html" src/app/(public)/[username]/[slug]/route.ts returns a match
    - grep "Trang đã bị lưu trữ" src/app/(public)/[username]/[slug]/route.ts returns a match
    - grep "htmlContent" src/app/(public)/[username]/[slug]/route.ts returns a match
    - npm run build exits 0
  </acceptance_criteria>
  <done>Public route serves raw HTML for published, 404 for draft, archived message for archived</done>
</task>

<task type="auto">
  <name>Task 2: Landing page rewrite + dashboard nav rebrand</name>
  <files>src/app/page.tsx, src/app/(dashboard)/dashboard/dashboard-nav.tsx</files>
  <read_first>
    - src/app/page.tsx (current placeholder to replace)
    - src/app/(dashboard)/dashboard/dashboard-nav.tsx (find brand text to change)
    - src/components/ui/button.tsx (for Button import path)
  </read_first>
  <action>
**1. Rewrite `src/app/page.tsx`** as a Server Component landing page.

Structure per UI-SPEC: Hero -> Features (3 cards) -> How it works (3 steps) -> Footer.

- Import `Button` from `@/components/ui/button` and `Link` from `next/link`
- Use `motion` from `motion/react` for hero entry animation per UI-SPEC:
  - `initial: { opacity: 0, y: 16 }` -> `animate: { opacity: 1, y: 0 }`, duration 0.4s, stagger 0.1s
  - NOTE: Since motion requires "use client", create a `LandingHero` client component inline or at module scope within the file with "use client" directive, or make the whole page "use client"
  - Simpler approach: make page.tsx a Server Component, animation is nice-to-have. Use CSS animations instead: `@keyframes fadeInUp` with `animation-delay` for stagger. This keeps the page a Server Component for SEO.

Hero section:
- Headline: "Biến ghi chú thành website trong vài giây" (text-4xl sm:text-5xl font-semibold)
- Subtext: "Mô tả app của bạn, AI sẽ tạo ra một website hoàn chỉnh — không cần code." (text-lg text-muted-foreground)
- CTA 1: `<Button asChild><Link href="/register">Bắt đầu miễn phí</Link></Button>` (default variant)
- CTA 2: `<Button variant="outline" asChild><Link href="/login">Đăng nhập</Link></Button>`

Features section (title: "Tại sao chọn AppGen?"):
- 3 cards in `grid grid-cols-1 md:grid-cols-3 gap-6` with `hover:shadow-md transition-shadow duration-200`
- Card 1: "AI tạo website" — "Mô tả ý tưởng, AI sẽ tạo website hoàn chỉnh với HTML, CSS và JavaScript."
- Card 2: "Chỉnh sửa trực quan" — "Giao diện editor giống Lovable — xem trước trực tiếp và chỉnh sửa bằng chat."
- Card 3: "Xuất bản tức thì" — "Một click để website của bạn có URL công khai, sẵn sàng chia sẻ."

How it works section (title: "Cách hoạt động"):
- 3 steps in grid: Step 1 "Mô tả website", Step 2 "AI tạo nội dung", Step 3 "Xuất bản và chia sẻ"

Footer:
- Simple `text-center text-sm text-muted-foreground` with "AppGen — Tạo website bằng AI" and current year

Full page wrapped in `max-w-6xl mx-auto px-6` container. Vietnamese throughout.

**2. Dashboard nav rebrand** in `src/app/(dashboard)/dashboard/dashboard-nav.tsx`:
Find the text "Website Generator" (or whatever the current brand text is) and replace with "AppGen".
  </action>
  <verify>
    <automated>npm run typecheck && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - grep "Bắt đầu miễn phí" src/app/page.tsx returns a match
    - grep "Tại sao chọn AppGen" src/app/page.tsx returns a match
    - grep "Cách hoạt động" src/app/page.tsx returns a match
    - grep "/register" src/app/page.tsx returns a match (hero CTA link)
    - grep "/login" src/app/page.tsx returns a match (hero CTA link)
    - grep "AppGen" src/app/(dashboard)/dashboard/dashboard-nav.tsx returns a match
    - grep -i "Website Generator" src/app/(dashboard)/dashboard/dashboard-nav.tsx returns NO match
    - npm run typecheck exits 0
  </acceptance_criteria>
  <done>Landing page shows full Vietnamese content with hero/features/how-it-works/footer; dashboard nav shows AppGen brand</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds (no page.tsx/route.ts conflict)
- `npm run typecheck` passes
- Landing page renders at localhost:3000
</verification>

<success_criteria>
- Public route returns raw HTML for published websites via route.ts
- Draft websites get 404, archived get Vietnamese message
- Landing page has hero, features, how-it-works, footer — all Vietnamese
- Dashboard nav says "AppGen"
</success_criteria>

<output>
After completion, create `.planning/phases/07-html-first-ai-generation-and-lovable-style-editor/07-02-SUMMARY.md`
</output>
