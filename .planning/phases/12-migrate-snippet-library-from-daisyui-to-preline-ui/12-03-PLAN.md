---
phase: 12-migrate-snippet-library-from-daisyui-to-preline-ui
plan: 03
type: execute
wave: 1
depends_on: ["12-02"]
files_modified:
  - src/lib/component-library/snippets/blog.ts
  - src/lib/component-library/snippets/pricing.ts
  - src/lib/component-library/component-library.test.ts
autonomous: true
gap_closure: true
requirements:
  - SNIP-01
  - SNIP-02
  - SNIP-03
  - SNIP-04
  - SNIP-05
  - SNIP-06
  - SNIP-07
  - SNIP-08

must_haves:
  truths:
    - "Zero hs-accordion-active: variant classes in any snippet file"
    - "DaisyUI remnant detector test also catches banned hs-* variant classes"
    - "Accordion chevron rotation works via inline JS class toggling instead of hs-accordion-active: CSS variant"
  artifacts:
    - path: "src/lib/component-library/snippets/blog.ts"
      provides: "Accordion chevron rotation via vanilla JS"
      contains: "classList"
    - path: "src/lib/component-library/snippets/pricing.ts"
      provides: "FAQ accordion chevron rotation via vanilla JS"
      contains: "classList"
    - path: "src/lib/component-library/component-library.test.ts"
      provides: "Banned hs-* variant pattern detector"
      contains: "hs-accordion-active:"
  key_links:
    - from: "src/lib/component-library/component-library.test.ts"
      to: "ALL_SNIPPETS"
      via: "forEach pattern check"
      pattern: "hs-accordion-active"
---

<objective>
Fix the single gap found in Phase 12 verification: replace 5 occurrences of the banned `hs-accordion-active:rotate-180` CSS variant class with vanilla JS class toggling, and add `hs-accordion-active:` to the test's banned pattern detector to prevent regressions.

Purpose: `hs-accordion-active:` is a Preline variant class that requires a build step — it does NOT work via CDN. The accordion expand/collapse works (via `data-hs-*`), but the chevron rotation animation is silently broken in generated websites.

Output: 3 modified files, all 32+ tests passing, zero `hs-accordion-active:` in snippet library.
</objective>

<execution_context>
@D:/STEVE/steve/.claude/get-shit-done/workflows/execute-plan.md
@D:/STEVE/steve/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-migrate-snippet-library-from-daisyui-to-preline-ui/12-VERIFICATION.md

<interfaces>
<!-- The accordion pattern in both files uses the same structure: -->
<!-- An SVG inside a .hs-accordion-toggle button, with class="hs-accordion-active:rotate-180 ..." -->
<!-- The parent .hs-accordion div gets class "active" toggled by Preline JS when expanded -->
<!-- Solution: use a MutationObserver or Preline's afterTransition callback, OR simpler: -->
<!-- add an onclick listener to each .hs-accordion-toggle button that toggles a "rotate-180" class on its child SVG -->

From blog.ts line 353 (1 occurrence):
```html
<svg class="hs-accordion-active:rotate-180 size-4 text-gray-600 dark:text-gray-300 transition-transform" ...>
```

From pricing.ts lines 214, 223, 232, 241 (4 occurrences):
```html
<svg class="hs-accordion-active:rotate-180 size-4 text-gray-600 dark:text-gray-300 shrink-0 transition-transform" ...>
```

From component-library.test.ts lines 31-51:
```typescript
it("no snippet contains DaisyUI class patterns", () => {
  const daisyUIPatterns = [
    'btn-primary', 'btn-secondary', /* ... 28 patterns total ... */
  ];
  ALL_SNIPPETS.forEach((s: ComponentSnippet) => {
    daisyUIPatterns.forEach((pattern) => {
      expect(s.html, `${s.id}: contains DaisyUI pattern "${pattern}"`).not.toContain(pattern);
    });
  });
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hs-accordion-active:rotate-180 with vanilla JS class toggling in blog.ts and pricing.ts</name>
  <files>src/lib/component-library/snippets/blog.ts, src/lib/component-library/snippets/pricing.ts</files>
  <read_first>
    - src/lib/component-library/snippets/blog.ts (lines 340-370 — the table-of-contents accordion snippet)
    - src/lib/component-library/snippets/pricing.ts (lines 200-250 — the pricing-faq accordion snippet)
  </read_first>
  <action>
In BOTH files, the fix follows the same pattern. The `hs-accordion-active:rotate-180` CSS variant does not work via CDN. Replace it with vanilla JS that listens for Preline's accordion toggle and applies `rotate-180` via classList.

**blog.ts (1 SVG at line 353):**

1. On the SVG element at line 353, remove `hs-accordion-active:rotate-180` from the class attribute. The SVG class should become:
   `class="size-4 text-gray-600 dark:text-gray-300 transition-transform" ...`

2. After the closing `</div>` of the accordion section (after `</section>`), add an inline `<script>` block that toggles the rotation:

```html
<script>
document.querySelectorAll('.hs-accordion-toggle').forEach(function(btn){
  btn.addEventListener('click', function(){
    var svg = this.querySelector('svg');
    if(svg) svg.classList.toggle('rotate-180');
  });
});
</script>
```

Place this script INSIDE the template literal, right before the closing backtick of the html property. This ensures Preline JS handles the accordion expand/collapse while our script handles the chevron rotation.

**pricing.ts (4 SVGs at lines 214, 223, 232, 241):**

1. On ALL FOUR SVG elements, remove `hs-accordion-active:rotate-180` from the class attribute. Each SVG class should become:
   `class="size-4 text-gray-600 dark:text-gray-300 shrink-0 transition-transform" ...`

2. The first accordion item (id="faq-1") has class `active` by default, so its SVG should start with `rotate-180` in its class list:
   `class="rotate-180 size-4 text-gray-600 dark:text-gray-300 shrink-0 transition-transform" ...`
   The other 3 SVGs (faq-2, faq-3, faq-4) do NOT get `rotate-180` initially since those accordions start closed.

3. After the closing `</section>` tag but still inside the template literal, add the same inline `<script>` block:

```html
<script>
document.querySelectorAll('.hs-accordion-toggle').forEach(function(btn){
  btn.addEventListener('click', function(){
    var svg = this.querySelector('svg');
    if(svg) svg.classList.toggle('rotate-180');
  });
});
</script>
```

**For blog.ts as well:** The blog accordion starts with class `active` on the `hs-accordion` div, so its SVG should also start with `rotate-180`:
   `class="rotate-180 size-4 text-gray-600 dark:text-gray-300 transition-transform" ...`

CRITICAL: After editing, grep both files to confirm ZERO occurrences of `hs-accordion-active:` remain.
  </action>
  <verify>
    <automated>cd D:/STEVE/steve && grep -c "hs-accordion-active:" src/lib/component-library/snippets/blog.ts src/lib/component-library/snippets/pricing.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep "hs-accordion-active:" blog.ts returns 0 matches
    - grep "hs-accordion-active:" pricing.ts returns 0 matches
    - grep "classList.toggle" blog.ts returns >= 1 match (vanilla JS toggling present)
    - grep "classList.toggle" pricing.ts returns >= 1 match (vanilla JS toggling present)
    - grep "rotate-180" blog.ts returns >= 1 match (initial state for active accordion)
    - grep "rotate-180" pricing.ts returns >= 1 match (initial state + transition class)
    - grep "hs-accordion-toggle" blog.ts returns >= 1 match (Preline accordion structure preserved)
    - grep "hs-accordion-toggle" pricing.ts returns >= 1 match (Preline accordion structure preserved)
  </acceptance_criteria>
  <done>Zero hs-accordion-active: occurrences in blog.ts and pricing.ts. Accordion chevron rotation handled by vanilla JS classList.toggle('rotate-180') on click. Active-by-default accordions start with rotate-180 class.</done>
</task>

<task type="auto">
  <name>Task 2: Add banned hs-* variant patterns to the remnant detector test</name>
  <files>src/lib/component-library/component-library.test.ts</files>
  <read_first>
    - src/lib/component-library/component-library.test.ts (lines 31-51 — the DaisyUI remnant detector test)
  </read_first>
  <action>
In `component-library.test.ts`, the test "no snippet contains DaisyUI class patterns" checks an array called `daisyUIPatterns`. This test must be extended to also catch banned Preline hs-* variant classes that require a build step.

1. Rename the test description from `"no snippet contains DaisyUI class patterns"` to `"no snippet contains banned class patterns (DaisyUI + hs-* variants)"` to reflect the expanded scope.

2. After the existing `daisyUIPatterns` array (which has 28 DaisyUI patterns), add a second array called `bannedHsVariants`:

```typescript
const bannedHsVariants = [
  'hs-accordion-active:',
  'hs-collapse-open:',
  'hs-overlay-open:',
  'hs-dropdown-open:',
  'hs-tab-active:',
  'hs-stepper-active:',
  'hs-stepper-completed:',
];
```

3. Combine both arrays into a single check. After the existing `daisyUIPatterns.forEach(...)` block, add:

```typescript
bannedHsVariants.forEach((pattern) => {
  expect(s.html, `${s.id}: contains banned hs-* variant "${pattern}"`).not.toContain(pattern);
});
```

This goes inside the existing `ALL_SNIPPETS.forEach((s) => { ... })` block, right after the daisyUIPatterns forEach.

NOTE: This test will ALSO catch the hs-stepper-active: and hs-stepper-completed: patterns in forms.ts and interactive.ts. Those files were NOT flagged in the VERIFICATION gap but DO contain these patterns. If the test fails on those files, the executor must ALSO fix forms.ts and interactive.ts using the same vanilla JS approach (replace hs-stepper-active:/hs-stepper-completed: with classList-based toggling). This is expected — the test is designed to catch ALL banned patterns.

4. Run `npm run test` to verify all tests pass. If forms.ts or interactive.ts cause failures, fix them using the same classList.toggle approach before proceeding.
  </action>
  <verify>
    <automated>cd D:/STEVE/steve && npm run test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "bannedHsVariants" component-library.test.ts returns >= 1 match
    - grep "hs-accordion-active:" component-library.test.ts returns >= 1 match
    - grep "hs-stepper-active:" component-library.test.ts returns >= 1 match
    - grep "banned hs-\\* variant" component-library.test.ts returns >= 1 match (test description updated)
    - npm run test exits with code 0 (all tests pass)
    - grep -r "hs-accordion-active:" src/lib/component-library/snippets/ returns 0 matches
  </acceptance_criteria>
  <done>Banned hs-* variant detector test added to component-library.test.ts. Test checks for hs-accordion-active:, hs-collapse-open:, hs-overlay-open:, hs-dropdown-open:, hs-tab-active:, hs-stepper-active:, hs-stepper-completed:. All tests pass — zero banned patterns remain in any snippet file.</done>
</task>

</tasks>

<verification>
1. `grep -r "hs-accordion-active:" src/lib/component-library/snippets/` returns zero results
2. `npm run test` passes all tests (including the new banned hs-* variant detector)
3. `npm run typecheck` passes clean
4. Accordion snippets in blog.ts and pricing.ts contain inline `<script>` with classList.toggle for chevron rotation
</verification>

<success_criteria>
- Zero occurrences of `hs-accordion-active:` in blog.ts and pricing.ts
- Vanilla JS classList.toggle('rotate-180') handles chevron animation in accordion snippets
- Banned hs-* variant patterns added to test suite and all tests pass
- npm run test and npm run typecheck pass clean
</success_criteria>

<output>
After completion, create `.planning/phases/12-migrate-snippet-library-from-daisyui-to-preline-ui/12-03-SUMMARY.md`
</output>
