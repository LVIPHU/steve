---
phase: 12-migrate-snippet-library-from-daisyui-to-preline-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/component-library/component-library.test.ts
  - src/lib/html-prompts.ts
  - src/lib/component-library/snippets/hero.ts
  - src/lib/component-library/snippets/navbar.ts
  - src/lib/component-library/snippets/features.ts
  - src/lib/component-library/snippets/cards.ts
  - src/lib/component-library/snippets/footer.ts
autonomous: true
requirements: [SNIP-01, SNIP-04, SNIP-05, SNIP-06, SNIP-08]

must_haves:
  truths:
    - "buildSystemPrompt() returns a prompt mentioning Tailwind CDN and Preline JS CDN, with zero DaisyUI references"
    - "Hero, navbar, features, cards, footer snippets contain only Tailwind utility classes and Preline data-hs-* attributes, zero DaisyUI class names"
    - "All 5 high-impact snippet files have 2-3 new snippets added beyond original count"
    - "Test file has threshold 100 and a DaisyUI remnant detector test"
    - "All snippets include dark: prefix classes for dark mode support"
  artifacts:
    - path: "src/lib/html-prompts.ts"
      provides: "Rewritten buildSystemPrompt with Preline guidance"
      contains: "cdn.tailwindcss.com"
    - path: "src/lib/component-library/snippets/hero.ts"
      provides: "6-7 hero snippets with Tailwind+Preline"
      min_lines: 80
    - path: "src/lib/component-library/snippets/navbar.ts"
      provides: "5-6 navbar snippets with Preline collapse"
      min_lines: 80
    - path: "src/lib/component-library/snippets/features.ts"
      provides: "6-7 feature snippets with Tailwind"
      min_lines: 80
    - path: "src/lib/component-library/snippets/cards.ts"
      provides: "6-7 card snippets with Tailwind"
      min_lines: 80
    - path: "src/lib/component-library/snippets/footer.ts"
      provides: "5-6 footer snippets with Tailwind"
      min_lines: 80
    - path: "src/lib/component-library/component-library.test.ts"
      provides: "Updated test thresholds and DaisyUI remnant detector"
      contains: "toBeGreaterThanOrEqual(100)"
  key_links:
    - from: "src/lib/html-prompts.ts"
      to: "buildSystemPrompt"
      via: "zero-parameter function signature"
      pattern: "export function buildSystemPrompt\\(\\): string"
    - from: "src/lib/component-library/snippets/hero.ts"
      to: "src/lib/component-library/snippets/index.ts"
      via: "heroSnippets export"
      pattern: "export const heroSnippets"
---

<objective>
Rewrite the 5 highest-impact snippet categories (hero, navbar, features, cards, footer) from DaisyUI to Tailwind utilities + Preline data-hs-* patterns. Rewrite buildSystemPrompt() to reference Tailwind CDN + Preline JS CDN with full Preline guidance. Update test thresholds and add DaisyUI remnant detector.

Purpose: These 5 categories are the most commonly selected by selectComponents(). Getting them right first ensures the majority of generated websites immediately benefit from the migration.

Output: 7 rewritten files — 1 test file, 1 prompt file, 5 snippet files with expanded snippets.
</objective>

<execution_context>
@D:/STEVE/steve/.claude/get-shit-done/workflows/execute-plan.md
@D:/STEVE/steve/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-migrate-snippet-library-from-daisyui-to-preline-ui/12-CONTEXT.md
@.planning/phases/12-migrate-snippet-library-from-daisyui-to-preline-ui/12-RESEARCH.md
@.planning/research/PRELINE-UI.md

<interfaces>
<!-- ComponentSnippet type — all snippets must conform -->
From src/lib/component-library/types.ts:
```typescript
export interface ComponentSnippet {
  id: string;
  name: string;
  description: string;
  category: string;
  tags: string[];
  priority: number;
  domain_hints: string[];
  min_score: number;
  fallback: boolean;
  fallback_for: string[];
  html: string;
}
```

From src/lib/html-prompts.ts (current signature — MUST keep zero-parameter):
```typescript
export function buildSystemPrompt(): string { ... }
export function stripMarkdownFences(raw: string): string { ... }
```

From src/lib/component-library/snippets/index.ts (registry — Plan 02 will add new imports):
```typescript
import { heroSnippets } from "./hero";
import { navbarSnippets } from "./navbar";
import { featuresSnippets } from "./features";
import { cardsSnippets } from "./cards";
import { footerSnippets } from "./footer";
// ... other imports ...
export const ALL_SNIPPETS: ComponentSnippet[] = [
  ...heroSnippets, ...navbarSnippets, ...featuresSnippets,
  ...cardsSnippets, ...footerSnippets, ...statsSnippets,
  ...testimonialsSnippets, ...interactiveSnippets, ...blogSnippets,
  ...portfolioSnippets, ...ecommerceSnippets,
];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Update test thresholds and add DaisyUI remnant detector</name>
  <files>src/lib/component-library/component-library.test.ts</files>
  <read_first>
    - src/lib/component-library/component-library.test.ts
  </read_first>
  <action>
1. On line ~27, change `toBeGreaterThanOrEqual(40)` to `toBeGreaterThanOrEqual(100)`.

2. Add a new test inside the "snippet data validation" describe block:

```typescript
it("no snippet html contains DaisyUI class names", () => {
  const daisyClasses = [
    'class="btn ', 'class="card ', 'class="badge ',
    'navbar-start', 'navbar-end', 'navbar-center',
    'hero-content', 'stat-title', 'stat-value', 'stat-desc',
    'footer-center', 'mockup', 'daisyui',
    'btn-primary', 'btn-secondary', 'btn-outline', 'btn-ghost',
    'btn-sm', 'btn-lg', 'btn-error', 'btn-success', 'btn-warning',
    'card-body', 'card-title', 'card-actions',
    'alert-', 'form-control', 'input-bordered',
    'select-bordered', 'textarea-bordered',
    'bg-base-100', 'bg-base-200', 'bg-base-300',
    'text-base-content', 'base-content',
    'bg-neutral', 'text-neutral-content',
    'rounded-box', 'menu-horizontal', 'menu-sm',
    'dropdown-content', 'link-hover', 'link ',
  ];
  ALL_SNIPPETS.forEach((s: ComponentSnippet) => {
    daisyClasses.forEach((cls) => {
      expect(s.html, `${s.id}: html contains DaisyUI class "${cls}"`).not.toContain(cls);
    });
  });
});
```

This test will initially FAIL (expected — existing snippets still have DaisyUI). The remaining tasks in this plan and Plan 02 will fix all snippets to pass.
  </action>
  <verify>
    <automated>grep -c "toBeGreaterThanOrEqual(100)" src/lib/component-library/component-library.test.ts | grep -q "1" && grep -c "DaisyUI class" src/lib/component-library/component-library.test.ts | grep -q "1" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File contains `toBeGreaterThanOrEqual(100)` (exactly 1 occurrence)
    - File contains test name `no snippet html contains DaisyUI class names`
    - File contains string `daisyui` inside the daisyClasses array
    - File contains string `bg-base-100` inside the daisyClasses array
    - File contains string `btn-primary` inside the daisyClasses array
    - File contains string `card-body` inside the daisyClasses array
  </acceptance_criteria>
  <done>Test file has snippet count threshold of 100 and a comprehensive DaisyUI remnant detector test covering all known DaisyUI class patterns</done>
</task>

<task type="auto">
  <name>Task 1: Rewrite buildSystemPrompt() for Preline + Tailwind CDN</name>
  <files>src/lib/html-prompts.ts</files>
  <read_first>
    - src/lib/html-prompts.ts
    - .planning/phases/12-migrate-snippet-library-from-daisyui-to-preline-ui/12-CONTEXT.md
    - .planning/research/PRELINE-UI.md
  </read_first>
  <action>
Completely rewrite the `buildSystemPrompt()` function body. Keep the EXACT signature `export function buildSystemPrompt(): string`. Keep `stripMarkdownFences` unchanged.

The new system prompt string must contain ALL of the following sections (in English):

**1. Role + output format:**
```
You are an expert web developer. Generate a complete, self-contained website as a single HTML file with ALL CSS in <style> tags and ALL JavaScript in <script> tags.
```

**2. CDN setup (MUST appear exactly as written):**
```
ALWAYS include these CDNs in <head>:
  <script src="https://cdn.tailwindcss.com"></script>
  <script src="https://cdn.jsdelivr.net/npm/preline/dist/preline.js"></script>
```

**3. Styling rules:**
```
Styling rules:
- Use Tailwind utility classes for ALL styling — no DaisyUI, no Bootstrap, no custom CSS frameworks
- Mobile-first responsive design (sm:, md:, lg:, xl: breakpoints)
- Support dark mode with dark: prefix on all elements (dark:bg-gray-900, dark:text-white, dark:border-gray-700, etc.)
- Use the Design Brief CSS variables (--color-primary, --color-secondary, --color-accent, --color-bg) and Google Fonts @import at the top of your <style> block
- Common dark palette: page dark:bg-gray-900, cards dark:bg-gray-800, nested dark:bg-gray-700, text dark:text-white / dark:text-gray-300 / dark:text-gray-400, borders dark:border-gray-700
```

**4. Preline interactive behaviors:**
```
Preline UI interactive behaviors (powered by preline.js CDN):
- Mobile navbar toggle: add data-hs-collapse="#navbar-menu" on hamburger button, id="navbar-menu" on the collapsible menu div, use hs-collapse-open:block/hidden for visibility
- Accordion/FAQ: wrap in data-hs-accordion-group, each item has class="hs-accordion", toggle button has class="hs-accordion-toggle", content div has class="hs-accordion-content"
- Dropdown: wrapper has data-hs-dropdown, trigger has data-hs-dropdown-toggle, menu has class="hs-dropdown-menu" with hs-dropdown-open:opacity-100
- Modal/Overlay: trigger has data-hs-overlay="#modal-id", overlay div has id="modal-id" and class="hs-overlay hidden"
- Tabs: tab links have data-hs-tab="#panel-id", panels have id="panel-id" and role="tabpanel"
- Dark mode toggle: button with onclick that toggles document.documentElement.classList.toggle('dark'), saves to localStorage.setItem('theme', isDark ? 'dark' : 'light')
- Theme init script in <head>: if (localStorage.getItem('theme') === 'dark' || (!localStorage.getItem('theme') && window.matchMedia('(prefers-color-scheme: dark)').matches)) document.documentElement.classList.add('dark');
```

**5. Component patterns (Tailwind-only, NO DaisyUI):**
```
Component patterns (Tailwind utilities only — NO DaisyUI classes):
- Navbar: <nav class="bg-white dark:bg-gray-900 border-b border-gray-200 dark:border-gray-700 px-4 py-3"> with flex layout
- Hero: <section class="py-20 px-6"> or min-h-screen with centered content
- Cards: <div class="bg-white dark:bg-gray-800 border border-gray-200 dark:border-gray-700 rounded-xl shadow-sm p-6">
- Buttons: <a class="bg-blue-600 hover:bg-blue-700 text-white px-5 py-2.5 rounded-lg font-medium text-sm"> (primary) or border border-gray-300 (outline)
- Badges: <span class="bg-blue-100 text-blue-800 dark:bg-blue-900 dark:text-blue-300 text-xs font-medium px-2.5 py-0.5 rounded-full">
- Footer: <footer class="bg-gray-900 text-gray-300 py-12 px-6"> with grid columns
- Stats: <div class="text-center"><div class="text-3xl font-bold">1,240</div><div class="text-sm text-gray-500">Total Users</div></div>
- Progress bar: <div class="w-full bg-gray-200 dark:bg-gray-700 rounded-full h-2"><div class="bg-blue-600 h-2 rounded-full" style="width: 75%"></div></div>
```

**6. JavaScript rules:**
```
JavaScript rules:
- Use vanilla JavaScript for data-driven UIs (lists, arrays, quizzes, flashcards)
- For persistent state, use localStorage with "appgen-" prefix for all keys
- Do NOT use Alpine.js x-for — use vanilla JS DOM manipulation instead
- Never use alert(), confirm(), or prompt() — use Preline modal overlay pattern instead
- Preline JS auto-initializes on DOMContentLoaded — no manual init needed for data-hs-* components
```

**7. CSS anti-patterns:**
```
CSS rules — common mistakes to avoid:
- NEVER mix Tailwind utility names with CSS property syntax in <style> tags
- Flip card: wrapper MUST have explicit height (e.g., height: 220px), use perspective: 1000px, transform-style: preserve-3d, backface-visibility: hidden
- Do NOT use aspect-w-* or aspect-h-* Tailwind classes (requires plugin not in CDN)
- When writing custom CSS in <style>, use real CSS property names, never Tailwind class names
```

**8. Output format:**
```
Output ONLY raw HTML — no explanation, no markdown, no commentary.
Start response with <!DOCTYPE html>.
Adapt Component References from user message as structural inspiration — do not copy verbatim.
```

**9. Accessibility:**
```
Accessibility:
- Use semantic HTML5 elements (nav, main, section, article, aside, footer)
- Add aria-label on icon-only buttons
- Add role attributes where appropriate (role="region", role="tabpanel")
- Ensure sufficient color contrast in both light and dark modes
```
  </action>
  <verify>
    <automated>grep -c "cdn.tailwindcss.com" src/lib/html-prompts.ts | grep -q "1" && grep -c "preline" src/lib/html-prompts.ts | grep -q "[1-9]" && grep -c "daisyui" src/lib/html-prompts.ts | grep -q "0" && grep -c "DaisyUI" src/lib/html-prompts.ts | grep -q "0" && grep -c "export function buildSystemPrompt(): string" src/lib/html-prompts.ts | grep -q "1" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File contains exactly `export function buildSystemPrompt(): string` (zero-parameter invariant)
    - File contains `cdn.tailwindcss.com` (Tailwind CDN)
    - File contains `cdn.jsdelivr.net/npm/preline/dist/preline.js` (Preline CDN)
    - File contains ZERO occurrences of `daisyui` (case-insensitive)
    - File contains ZERO occurrences of `DaisyUI`
    - File contains `data-hs-collapse` (Preline navbar pattern)
    - File contains `data-hs-accordion-group` (Preline accordion pattern)
    - File contains `data-hs-overlay` (Preline modal pattern)
    - File contains `dark:bg-gray-900` (dark mode guidance)
    - File contains `localStorage.setItem('theme'` (dark mode persistence)
    - File contains `appgen-` (localStorage prefix rule)
    - File contains `stripMarkdownFences` function unchanged
  </acceptance_criteria>
  <done>buildSystemPrompt() is fully rewritten with Preline+Tailwind CDN setup, zero DaisyUI references, comprehensive Preline data-hs-* guidance, dark mode instructions, and accessibility hints. Zero-parameter signature preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite hero.ts, navbar.ts, features.ts, cards.ts, footer.ts with Tailwind+Preline snippets</name>
  <files>
    src/lib/component-library/snippets/hero.ts
    src/lib/component-library/snippets/navbar.ts
    src/lib/component-library/snippets/features.ts
    src/lib/component-library/snippets/cards.ts
    src/lib/component-library/snippets/footer.ts
  </files>
  <read_first>
    - src/lib/component-library/snippets/hero.ts
    - src/lib/component-library/snippets/navbar.ts
    - src/lib/component-library/snippets/features.ts
    - src/lib/component-library/snippets/cards.ts
    - src/lib/component-library/snippets/footer.ts
    - src/lib/component-library/types.ts
    - .planning/research/PRELINE-UI.md
    - .planning/phases/12-migrate-snippet-library-from-daisyui-to-preline-ui/12-CONTEXT.md
  </read_first>
  <action>
Rewrite ALL 5 files. Each file: rewrite every existing snippet HTML replacing ALL DaisyUI classes with Tailwind utilities + Preline data-hs-* patterns. Then add 2-3 NEW snippets per file. Keep ALL existing snippet metadata (id, name, description, category, tags, priority, domain_hints, min_score, fallback, fallback_for) identical — only change `html` content. New snippets get unique ids and appropriate metadata.

**CRITICAL COLOR RULE:** Use Tailwind palette classes (blue-600, gray-900, etc.) as placeholders. NO DaisyUI semantic classes (bg-base-100, text-base-content, btn-primary, etc.). NO Preline semantic tokens (bg-primary, text-foreground, bg-layer, etc.).

**CRITICAL DARK MODE RULE:** Every snippet must include dark: prefix variants. Page backgrounds: `bg-white dark:bg-gray-900`. Cards: `bg-white dark:bg-gray-800`. Text: `text-gray-900 dark:text-white` / `text-gray-600 dark:text-gray-400`. Borders: `border-gray-200 dark:border-gray-700`.

### hero.ts — Rewrite 4 existing + add 2-3 new (target: 6-7 snippets)

**hero-centered:** Replace `class="hero min-h-screen bg-base-200"` with `class="min-h-screen bg-gray-50 dark:bg-gray-900 flex items-center justify-center"`. Replace `hero-content text-center` with `text-center max-w-2xl mx-auto px-6`. Replace `btn btn-primary btn-lg` with `bg-blue-600 hover:bg-blue-700 text-white px-6 py-3 rounded-lg font-medium`. Replace `btn btn-outline btn-lg` with `border border-gray-300 dark:border-gray-600 text-gray-700 dark:text-gray-300 hover:bg-gray-100 dark:hover:bg-gray-800 px-6 py-3 rounded-lg font-medium`. Replace `text-base-content/70` with `text-gray-600 dark:text-gray-400`.

**hero-split:** Replace `class="hero min-h-screen bg-base-100"` with `class="min-h-screen bg-white dark:bg-gray-900 flex items-center"`. Replace `hero-content flex-col lg:flex-row` with `flex flex-col lg:flex-row`. Replace `badge badge-primary` with `inline-flex items-center bg-blue-100 text-blue-800 dark:bg-blue-900 dark:text-blue-300 text-xs font-medium px-2.5 py-0.5 rounded-full`. Replace `btn btn-primary` with `bg-blue-600 hover:bg-blue-700 text-white px-5 py-2.5 rounded-lg font-medium`. Replace `btn btn-ghost` with `text-gray-700 dark:text-gray-300 hover:bg-gray-100 dark:hover:bg-gray-800 px-5 py-2.5 rounded-lg font-medium`. Replace `text-primary` with `text-blue-600 dark:text-blue-400`. Replace gradient `from-primary to-secondary` with `from-blue-600 to-indigo-600`.

**hero-minimal:** Replace `badge badge-outline badge-lg` with `inline-flex items-center border border-gray-300 dark:border-gray-600 text-gray-700 dark:text-gray-300 text-sm font-medium px-3 py-1 rounded-full`. Replace `text-primary` with `text-blue-600 dark:text-blue-400`. Replace `btn btn-primary btn-lg` with `bg-blue-600 hover:bg-blue-700 text-white px-8 py-3 rounded-lg font-medium text-lg`. Replace `text-base-content/60` with `text-gray-500 dark:text-gray-400`.

**hero-dashboard:** Replace `bg-base-200` with `bg-gray-50 dark:bg-gray-900`. Replace `text-base-content/60` with `text-gray-500 dark:text-gray-400`. Replace `btn btn-ghost btn-sm` with `text-gray-600 dark:text-gray-400 hover:bg-gray-100 dark:hover:bg-gray-800 px-3 py-1.5 rounded-lg text-sm font-medium`. Replace `btn btn-primary btn-sm` with `bg-blue-600 hover:bg-blue-700 text-white px-3 py-1.5 rounded-lg text-sm font-medium`. Replace `stat bg-base-100 rounded-box` with `bg-white dark:bg-gray-800 border border-gray-200 dark:border-gray-700 rounded-xl`. Replace `stat-title` with a `<div class="text-sm text-gray-500 dark:text-gray-400">`. Replace `stat-value` with `<div class="text-2xl font-bold text-gray-900 dark:text-white">`. Replace `stat-desc text-success` with `<div class="text-sm text-green-600 dark:text-green-400">`. Replace `stat-desc text-error` with `<div class="text-sm text-red-600 dark:text-red-400">`. Replace `stat-desc` (plain) with `<div class="text-sm text-gray-500 dark:text-gray-400">`.

**NEW hero-video-bg:** id="hero-video-bg", category="hero", tags=["hero", "cta"], priority=2, domain_hints=["landing"], fallback=false. HTML: full-width section with a placeholder video background div (gradient simulating video), overlay, centered text with h1 + subtitle + CTA button. Use `relative min-h-screen flex items-center justify-center bg-gradient-to-br from-gray-900 to-blue-900`, white text, blue-600 CTA button.

**NEW hero-announcement-bar:** id="hero-announcement-bar", category="hero", tags=["hero"], priority=2, domain_hints=[], fallback=false. HTML: a top announcement bar `<div class="bg-blue-600 text-white text-center text-sm py-2 px-4">` with a dismissible X button, followed by a standard centered hero section below it. Use `data-hs-remove-element` for the dismiss behavior.

### navbar.ts — Rewrite 3 existing + add 2-3 new (target: 5-6 snippets)

**navbar-simple:** Replace `navbar bg-base-100 shadow-sm` with `bg-white dark:bg-gray-900 border-b border-gray-200 dark:border-gray-700`. Replace `navbar-start/center/end` with flex layout: `flex items-center justify-between max-w-7xl mx-auto px-4 py-3`. Replace `text-primary` with `text-blue-600 dark:text-blue-400`. Replace `menu menu-horizontal` links with `<div class="hidden lg:flex items-center gap-6">` containing `<a class="text-sm font-medium text-gray-700 dark:text-gray-300 hover:text-blue-600 dark:hover:text-blue-400">`. Replace `btn btn-primary btn-sm` with `bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded-lg text-sm font-medium`.

**navbar-dropdown:** Replace entire structure. Use `bg-white dark:bg-gray-900 border-b border-gray-200 dark:border-gray-700`. Mobile hamburger: replace DaisyUI dropdown with Preline collapse: `<button data-hs-collapse="#navbar-menu" class="lg:hidden p-2 text-gray-600 dark:text-gray-400">` + `<div id="navbar-menu" class="hidden lg:flex ...">`. Desktop dropdown: use `<div class="hs-dropdown relative inline-flex">` with `data-hs-dropdown`, trigger button, and `<div class="hs-dropdown-menu hidden hs-dropdown-open:opacity-100 ...">`. Replace all DaisyUI classes with Tailwind utilities.

**navbar-mobile:** Replace the entire custom JS toggle with Preline `data-hs-collapse`. Replace `navbar bg-base-100 shadow-sm` with `bg-white dark:bg-gray-900 border-b border-gray-200 dark:border-gray-700`. The hamburger button gets `data-hs-collapse="#mobile-nav-menu"`. The mobile menu div gets `id="mobile-nav-menu" class="hs-collapse hidden overflow-hidden transition-all duration-300 lg:block"`. Remove the inline `<script>` block entirely — Preline JS handles it. Replace `link link-hover` with Tailwind text/hover classes. Replace `btn btn-primary btn-sm` with Tailwind button classes.

**NEW navbar-centered-logo:** id="navbar-centered-logo", category="navbar", tags=["navbar"], priority=2, domain_hints=[], fallback=false. HTML: logo centered with nav links on left and right sides. Use `grid grid-cols-3` layout. Left: nav links. Center: logo. Right: CTA button. Mobile: Preline collapse hamburger.

**NEW navbar-mega-menu:** id="navbar-mega-menu", category="navbar", tags=["navbar", "features"], priority=2, domain_hints=[], fallback=false. HTML: navbar with a Preline dropdown that opens a full-width mega menu panel. Use `<div class="hs-dropdown [--strategy:static] md:[--strategy:absolute]">` wrapper with `data-hs-dropdown`. Menu content: 3-column grid with category headings and links.

### features.ts — Rewrite 4 existing + add 2-3 new (target: 6-7 snippets)

Read the current features.ts file. For EACH existing snippet, replace ALL DaisyUI classes:
- `bg-base-100/200/300` → `bg-white dark:bg-gray-900` / `bg-gray-50 dark:bg-gray-900` / `bg-gray-100 dark:bg-gray-800`
- `card bg-base-*` → `bg-white dark:bg-gray-800 border border-gray-200 dark:border-gray-700 rounded-xl shadow-sm`
- `text-base-content/60` → `text-gray-600 dark:text-gray-400`
- `text-primary` → `text-blue-600 dark:text-blue-400`
- `btn btn-*` → corresponding Tailwind button classes
- `badge badge-*` → Tailwind badge spans

**NEW features-with-image:** id="features-with-image", category="features", tags=["features"], priority=2, domain_hints=[], fallback=false. HTML: alternating rows — text left/image right, then image left/text right. Use `flex flex-col md:flex-row items-center gap-8` with `md:flex-row-reverse` on alternating rows. Image placeholder: gradient div.

**NEW features-tabs:** id="features-tabs", category="features", tags=["features", "tabs"], priority=2, domain_hints=[], fallback=false. HTML: Preline tabs UI. Tab bar with 3-4 tabs using `data-hs-tab="#panel-1"`. Each panel shows a feature description + icon. Use `hs-tab-active:bg-blue-600 hs-tab-active:text-white` on tab buttons.

### cards.ts — Rewrite 4 existing + add 2-3 new (target: 6-7 snippets)

Read the current cards.ts file. For EACH existing snippet:
- Replace `card bg-base-200 shadow-sm` with `bg-white dark:bg-gray-800 border border-gray-200 dark:border-gray-700 rounded-xl shadow-sm hover:shadow-md transition-shadow`
- Replace `card-body` with `p-6`
- Replace `card-title` with `text-lg font-semibold text-gray-900 dark:text-white`
- Replace `card-actions` with `flex justify-end mt-4`
- Replace `badge badge-outline badge-sm` with `text-xs font-medium border border-gray-300 dark:border-gray-600 text-gray-600 dark:text-gray-400 px-2 py-0.5 rounded-full`
- Replace `btn btn-primary btn-sm` with `bg-blue-600 hover:bg-blue-700 text-white px-3 py-1.5 rounded-lg text-sm font-medium`
- Replace `bg-gradient-to-br from-primary/20 to-secondary/20` with `bg-gradient-to-br from-blue-100 to-indigo-100 dark:from-blue-900/30 dark:to-indigo-900/30`
- Replace `text-base-content/60` with `text-gray-600 dark:text-gray-400`

**NEW card-horizontal:** id="card-horizontal", category="cards", tags=["cards"], priority=2, domain_hints=[], fallback=false. HTML: horizontal card with image on left, content on right. Use `flex flex-col sm:flex-row` layout. Image placeholder: `w-full sm:w-48 h-48 bg-gradient-to-br from-blue-100 to-indigo-100 dark:from-blue-900/30 dark:to-indigo-900/30 rounded-t-xl sm:rounded-l-xl sm:rounded-tr-none`.

**NEW card-image-overlay:** id="card-image-overlay", category="cards", tags=["cards"], priority=2, domain_hints=["portfolio"], fallback=false. HTML: card where text overlays the image. Use `relative overflow-hidden rounded-xl` with gradient overlay div `absolute inset-0 bg-gradient-to-t from-black/80 to-transparent` and text positioned at bottom with `absolute bottom-0 p-6 text-white`.

### footer.ts — Rewrite 3 existing + add 2-3 new (target: 5-6 snippets)

Read the current footer.ts file. For EACH existing snippet:
- Replace `footer footer-center p-10 bg-base-200 text-base-content` with `bg-gray-100 dark:bg-gray-900 text-gray-600 dark:text-gray-400 py-12 px-6`
- Replace `footer bg-neutral text-neutral-content p-10` with `bg-gray-900 text-gray-300 py-12 px-6`
- Replace `text-primary` with `text-blue-600 dark:text-blue-400`
- Replace `link link-hover` with `text-gray-600 dark:text-gray-400 hover:text-gray-900 dark:hover:text-white transition-colors`
- Replace `text-base-content/60` and `text-base-content/40` with `text-gray-500 dark:text-gray-500`
- Remove any `footer-center` class usage — use flexbox/grid layout instead

**NEW footer-with-newsletter:** id="footer-with-newsletter", category="footer", tags=["footer", "newsletter"], priority=2, domain_hints=[], fallback=false. HTML: footer with a newsletter signup form at the top (email input + subscribe button), then link columns below. Use `max-w-7xl mx-auto`. Input: `bg-white dark:bg-gray-800 border border-gray-300 dark:border-gray-600 rounded-lg px-4 py-2`. Button: `bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded-lg`.

**NEW footer-app-store:** id="footer-app-store", category="footer", tags=["footer"], priority=2, domain_hints=["landing"], fallback=false. HTML: footer with app store badge placeholders (styled divs simulating App Store and Google Play buttons), social links, and copyright. Use `bg-gray-900 text-gray-300`.
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - hero.ts exports `heroSnippets` array with 6-7 items
    - hero.ts contains ZERO occurrences of `bg-base-` or `btn-primary` or `hero-content` or `badge badge-`
    - hero.ts contains `dark:bg-gray-900` and `dark:text-white`
    - hero.ts contains snippet with id `hero-video-bg`
    - hero.ts contains snippet with id `hero-announcement-bar`
    - hero.ts contains `data-hs-remove-element` (announcement bar dismiss)
    - navbar.ts exports `navbarSnippets` array with 5-6 items
    - navbar.ts contains ZERO occurrences of `navbar-start` or `navbar-center` or `navbar-end` or `menu menu-horizontal` or `btn btn-`
    - navbar.ts contains `data-hs-collapse` (Preline mobile toggle)
    - navbar.ts contains `data-hs-dropdown` (Preline dropdown)
    - navbar.ts contains snippet with id `navbar-centered-logo`
    - navbar.ts contains snippet with id `navbar-mega-menu`
    - features.ts contains ZERO occurrences of `bg-base-` or `btn-primary` or `card bg-`
    - features.ts contains snippet with id `features-with-image`
    - features.ts contains snippet with id `features-tabs`
    - features.ts contains `data-hs-tab` (Preline tabs)
    - cards.ts contains ZERO occurrences of `card-body` or `card-title` or `card-actions` or `badge badge-`
    - cards.ts contains snippet with id `card-horizontal`
    - cards.ts contains snippet with id `card-image-overlay`
    - footer.ts contains ZERO occurrences of `footer-center` or `link link-hover` or `bg-neutral` or `text-neutral-content`
    - footer.ts contains snippet with id `footer-with-newsletter`
    - footer.ts contains snippet with id `footer-app-store`
    - `npx tsc --noEmit` produces no errors for these files
  </acceptance_criteria>
  <done>All 5 high-impact snippet files fully rewritten with Tailwind utilities + Preline patterns, zero DaisyUI classes, dark mode support on every snippet, and 2-3 new snippets added per file. TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
After all 3 tasks complete:
1. `grep -r "bg-base-" src/lib/component-library/snippets/hero.ts src/lib/component-library/snippets/navbar.ts src/lib/component-library/snippets/features.ts src/lib/component-library/snippets/cards.ts src/lib/component-library/snippets/footer.ts` — must return empty (zero DaisyUI remnants in these 5 files)
2. `grep -c "daisyui\|DaisyUI" src/lib/html-prompts.ts` — must return 0
3. `npx tsc --noEmit` — must pass clean
4. Tests in Plan 01 scope will still FAIL until Plan 02 completes (remaining 6 files still have DaisyUI + snippet count < 100) — this is expected
</verification>

<success_criteria>
- buildSystemPrompt() fully rewritten: Tailwind+Preline CDN, zero DaisyUI, dark mode instructions, Preline data-hs-* patterns documented
- 5 snippet files (hero, navbar, features, cards, footer) fully migrated: zero DaisyUI classes, all dark: variants, Preline behaviors where applicable
- Each of the 5 files expanded with 2-3 new snippets
- Test file updated with threshold 100 and DaisyUI remnant detector
- TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/12-migrate-snippet-library-from-daisyui-to-preline-ui/12-01-SUMMARY.md`
</output>
