---
phase: 12-migrate-snippet-library-from-daisyui-to-preline-ui
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - src/lib/component-library/snippets/stats.ts
  - src/lib/component-library/snippets/testimonials.ts
  - src/lib/component-library/snippets/interactive.ts
  - src/lib/component-library/snippets/blog.ts
  - src/lib/component-library/snippets/portfolio.ts
  - src/lib/component-library/snippets/ecommerce.ts
  - src/lib/component-library/snippets/forms.ts
  - src/lib/component-library/snippets/ui-elements.ts
  - src/lib/component-library/snippets/cta.ts
  - src/lib/component-library/snippets/media.ts
  - src/lib/component-library/snippets/pricing.ts
  - src/lib/component-library/snippets/notifications.ts
autonomous: true
requirements:
  - SNIP-01
  - SNIP-02
  - SNIP-03
  - SNIP-05
  - SNIP-06
  - SNIP-07

must_haves:
  truths:
    - "ALL_SNIPPETS contains 100+ snippets"
    - "Zero DaisyUI class patterns in any snippet across all 17 files"
    - "At least 80% of all snippets include dark: prefix"
    - "Every fallback type (landing, portfolio, dashboard, blog, generic) still has >= 4 fallback snippets"
    - "All interactive snippet JS functionality preserved (quiz scoring, flashcard flip, timer start/pause/reset)"
    - "6 new categories each have 6-10 snippets"
    - "All snippet IDs are unique across the library"
  artifacts:
    - path: "src/lib/component-library/snippets/stats.ts"
      provides: "5-6 Preline/Tailwind stats snippets"
    - path: "src/lib/component-library/snippets/testimonials.ts"
      provides: "5-6 Preline/Tailwind testimonial snippets"
    - path: "src/lib/component-library/snippets/interactive.ts"
      provides: "8-9 Preline/Tailwind interactive snippets with preserved JS"
    - path: "src/lib/component-library/snippets/blog.ts"
      provides: "6-7 Preline/Tailwind blog snippets"
    - path: "src/lib/component-library/snippets/portfolio.ts"
      provides: "6-7 Preline/Tailwind portfolio snippets"
    - path: "src/lib/component-library/snippets/ecommerce.ts"
      provides: "6-7 Preline/Tailwind ecommerce snippets"
    - path: "src/lib/component-library/snippets/forms.ts"
      provides: "8-10 form snippets"
      contains: "forms-contact"
    - path: "src/lib/component-library/snippets/ui-elements.ts"
      provides: "8-10 UI element snippets"
      contains: "ui-elements-alerts"
    - path: "src/lib/component-library/snippets/cta.ts"
      provides: "8-10 CTA snippets"
      contains: "cta-banner"
    - path: "src/lib/component-library/snippets/media.ts"
      provides: "8-10 media snippets"
      contains: "media-image-grid"
    - path: "src/lib/component-library/snippets/pricing.ts"
      provides: "6-9 pricing snippets"
      contains: "pricing-three-tier"
    - path: "src/lib/component-library/snippets/notifications.ts"
      provides: "6-9 notification snippets"
      contains: "notifications-toast"
  key_links:
    - from: "src/lib/component-library/snippets/interactive.ts"
      to: "Quiz/Flashcard/Timer functionality"
      via: "Preserved inline <script> blocks"
      pattern: "setInterval|perspective.*1000px|appgen-quiz"
    - from: "ALL 17 snippet files"
      to: "src/lib/component-library/snippets/index.ts"
      via: "Named exports spread into ALL_SNIPPETS"
      pattern: "ALL_SNIPPETS"
---

<objective>
Rewrite the remaining 6 existing snippet files (stats, testimonials, interactive, blog, portfolio, ecommerce) and populate all 6 new category files (forms, ui-elements, cta, media, pricing, notifications) to reach 100+ total snippets with zero DaisyUI remnants.

Purpose: Complete the DaisyUI-to-Preline migration and expand the library to 100+ snippets across 17 categories so all Phase 12 tests pass green.

Output: 12 fully populated snippet files, all tests passing (including DaisyUI remnant detector, 100+ threshold, dark mode coverage), typecheck clean.
</objective>

<execution_context>
@D:/STEVE/steve/.claude/get-shit-done/workflows/execute-plan.md
@D:/STEVE/steve/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-migrate-snippet-library-from-daisyui-to-preline-ui/12-CONTEXT.md
@.planning/phases/12-migrate-snippet-library-from-daisyui-to-preline-ui/12-RESEARCH.md
@.planning/phases/12-migrate-snippet-library-from-daisyui-to-preline-ui/12-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 01 -->

From src/lib/component-library/types.ts:
```typescript
export interface ComponentSnippet {
  id: string;
  name: string;
  description: string;
  category: string;
  tags: string[];
  priority: number;
  domain_hints: string[];
  min_score: number;
  fallback: boolean;
  fallback_for: string[];
  html: string;
}
```

From 12-RESEARCH.md Pattern 4 — DaisyUI class replacement table (use these substitutions):
- `card bg-base-100 shadow-lg` -> `bg-white border border-gray-200 rounded-xl shadow-lg dark:bg-gray-800 dark:border-gray-700`
- `card-body` -> `p-6`
- `card-title` -> `text-xl font-bold text-gray-900 dark:text-white`
- `btn btn-primary` -> `py-2.5 px-5 inline-flex items-center text-sm font-medium rounded-lg bg-blue-600 text-white hover:bg-blue-700 transition-colors`
- `badge badge-primary` -> `inline-flex items-center py-1 px-2.5 rounded-full text-xs font-medium bg-blue-100 text-blue-800 dark:bg-blue-500/20 dark:text-blue-400`
- `bg-base-100` -> `bg-white dark:bg-gray-800`
- `bg-base-200` -> `bg-gray-100 dark:bg-gray-900`
- `text-base-content` -> `text-gray-900 dark:text-white`
- `stat-value` -> `text-2xl font-bold text-gray-900 dark:text-white mt-1`
- `stat-title` -> `text-sm text-gray-500 dark:text-gray-400`
- `stat-desc` -> `text-xs text-gray-400 dark:text-gray-500 mt-1`
- `progress progress-primary w-full` -> div-based: `<div class="flex w-full h-2 bg-gray-200 rounded-full dark:bg-gray-700"><div class="h-full bg-blue-600 rounded-full" style="width:70%"></div></div>`
- JS: `btn-success` -> `bg-teal-100 border-teal-300 text-teal-800 dark:bg-teal-500/20 dark:border-teal-900 dark:text-teal-300`
- JS: `btn-error` -> `bg-red-100 border-red-300 text-red-800 dark:bg-red-500/20 dark:border-red-900 dark:text-red-300`

Snippet count target per file after this plan:
- stats: 5-6, testimonials: 5-6, interactive: 8-9, blog: 6-7, portfolio: 6-7, ecommerce: 6-7
- forms: 8-10, ui-elements: 8-10, cta: 8-10, media: 8-10, pricing: 6-9, notifications: 6-9
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite 6 remaining existing snippet files (stats, testimonials, interactive, blog, portfolio, ecommerce)</name>
  <files>
    src/lib/component-library/snippets/stats.ts,
    src/lib/component-library/snippets/testimonials.ts,
    src/lib/component-library/snippets/interactive.ts,
    src/lib/component-library/snippets/blog.ts,
    src/lib/component-library/snippets/portfolio.ts,
    src/lib/component-library/snippets/ecommerce.ts
  </files>
  <read_first>
    src/lib/component-library/snippets/stats.ts,
    src/lib/component-library/snippets/testimonials.ts,
    src/lib/component-library/snippets/interactive.ts,
    src/lib/component-library/snippets/blog.ts,
    src/lib/component-library/snippets/portfolio.ts,
    src/lib/component-library/snippets/ecommerce.ts,
    src/lib/component-library/types.ts,
    src/lib/component-library/component-library.test.ts,
    .planning/phases/12-migrate-snippet-library-from-daisyui-to-preline-ui/12-RESEARCH.md
  </read_first>
  <action>
Rewrite all 6 files following the same pattern as Plan 01 Task 3. For each file:
1. **Preserve all metadata fields verbatim** on existing snippets — only `html` changes
2. **Replace all DaisyUI classes** using Pattern 4 replacement table
3. **Add dark: prefix classes** to every visual element
4. **Add 2-3 new snippets** per file
5. **Use category-prefixed IDs** for new snippets

**Per-file specifics:**

**stats.ts** (3 existing + 2-3 new = 5-6 total):
- Replace `stat bg-base-100 rounded-box p-4` with `bg-white border border-gray-200 rounded-xl p-4 dark:bg-gray-800 dark:border-gray-700`
- Replace `stat-title` with `text-sm text-gray-500 dark:text-gray-400`
- Replace `stat-value` with `text-2xl font-bold text-gray-900 dark:text-white mt-1`
- Replace `stat-desc` with `text-xs text-gray-400 dark:text-gray-500 mt-1`
- New additions: stats with progress bars (div-based, not DaisyUI `<progress>`), stats with Chart.js sparklines (canvas element, NO CDN link in html)

**testimonials.ts** (3 existing + 2-3 new = 5-6 total):
- Replace card classes with Tailwind equivalents
- New additions: testimonials with avatar group (stacked circles), testimonials with star ratings, testimonials carousel-style (vanilla JS auto-scroll)

**interactive.ts** (5 existing + 3-4 new = 8-9 total):
- **CRITICAL:** Preserve ALL existing JS functionality in <script> blocks — quiz scoring, flashcard 3D flip, timer start/pause/reset, calculator logic, progress tracker localStorage
- Replace CSS class names ONLY — both in HTML markup AND in JS strings:
  - `btn-success` in JS -> `bg-teal-100`, `border-teal-300`, `text-teal-800` (use classList.add with multiple classes)
  - `btn-error` in JS -> `bg-red-100`, `border-red-300`, `text-red-800`
  - `bg-base-100` -> `bg-white dark:bg-gray-800`
  - `hsl(var(--b1))` inline styles -> remove, use Tailwind `bg-white dark:bg-gray-800` class instead
- DaisyUI `<progress>` element -> div-based progress bar: `<div class="flex w-full h-2 bg-gray-200 rounded-full dark:bg-gray-700"><div class="h-full bg-blue-600 rounded-full transition-all duration-300"></div></div>`
- DaisyUI timeline -> flex column + left border approach
- `mockup-code` -> `<div class="bg-gray-900 rounded-lg p-4 font-mono text-sm text-gray-100">`
- Flashcard: replace `hsl(var(--b1))` inline style with class-based approach
- **Preserve these test-checked patterns:** `perspective: 1000px`, `height: 220px`, `backface-visibility: hidden`, `transform-style: preserve-3d`, `setInterval`, `Start`, `Pause`, `Reset`, `appgen-` prefix, `touchstart`, `mousedown`
- New additions: multi-step stepper wizard (`data-hs-stepper`), count-up animation on scroll (vanilla JS IntersectionObserver, NOT GSAP to avoid CDN in snippet), typing animation hero (vanilla JS)

**blog.ts** (4 existing + 2-3 new = 6-7 total):
- Replace all DaisyUI card/badge classes
- New additions: blog with Preline tabs for category filter (`data-hs-tab`), blog with accordion table of contents (`hs-accordion-group`), blog single article layout

**portfolio.ts** (4 existing + 2-3 new = 6-7 total):
- Replace all DaisyUI card classes
- New additions: portfolio with Preline tabs for filtering by type (`data-hs-tab`), portfolio modal lightbox (`data-hs-overlay`), portfolio masonry-like grid

**ecommerce.ts** (4 existing + 2-3 new = 6-7 total):
- Replace all DaisyUI card/badge/button classes
- New additions: product card with dismiss badge, cart summary with quantity increment/decrement (vanilla JS), product quick view modal (`data-hs-overlay`)

**SAME CRITICAL RULES AS PLAN 01:**
- NO CDN links in snippet HTML
- NO DOCTYPE in snippet HTML
- NO `bg-primary`, `text-foreground`, `bg-card` Preline semantic tokens
- NO `hs-accordion-active:`, `hs-collapse-open:` hs-* variant classes
- Preserve `fallback`, `fallback_for`, `id`, `name`, `description`, `category`, `tags`, `priority`, `domain_hints`, `min_score` on existing snippets
- All new snippet IDs must be unique (use category prefix: `stats-progress`, `testimonials-carousel`, etc.)
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - stats.ts contains at least 5 snippet objects
    - testimonials.ts contains at least 5 snippet objects
    - interactive.ts contains at least 8 snippet objects
    - blog.ts contains at least 6 snippet objects
    - portfolio.ts contains at least 6 snippet objects
    - ecommerce.ts contains at least 6 snippet objects
    - NONE of the 6 files contain: `btn-primary`, `card-body`, `bg-base-100`, `stat-value` (as DaisyUI class), `badge-primary`, `hero-content`, `footer-center`
    - ALL 6 files contain `dark:bg-gray` and `dark:text-`
    - interactive.ts contains `perspective: 1000px` (flashcard preserved)
    - interactive.ts contains `setInterval` (timer preserved)
    - interactive.ts contains `appgen-quiz` (quiz localStorage preserved)
    - interactive.ts contains `touchstart` (flashcard drag preserved)
    - interactive.ts contains `data-hs-stepper` (new stepper snippet)
    - interactive.ts does NOT contain `btn-success` as a string (DaisyUI JS class replaced)
    - interactive.ts does NOT contain `hsl(var(--b1))` (DaisyUI inline style removed)
    - blog.ts contains `data-hs-tab` or `hs-accordion` (Preline pattern)
    - portfolio.ts contains `data-hs-overlay` or `data-hs-tab` (Preline pattern)
    - `npm run typecheck` passes
  </acceptance_criteria>
  <done>6 remaining existing snippet files fully rewritten from DaisyUI to Tailwind/Preline. All interactive JS preserved. Each expanded with 2-4 new Preline-specific snippets. Combined total from all 11 existing categories: ~70-80 snippets.</done>
</task>

<task type="auto">
  <name>Task 2: Populate 6 new category files (forms, ui-elements, cta, media, pricing, notifications)</name>
  <files>
    src/lib/component-library/snippets/forms.ts,
    src/lib/component-library/snippets/ui-elements.ts,
    src/lib/component-library/snippets/cta.ts,
    src/lib/component-library/snippets/media.ts,
    src/lib/component-library/snippets/pricing.ts,
    src/lib/component-library/snippets/notifications.ts
  </files>
  <read_first>
    src/lib/component-library/snippets/forms.ts,
    src/lib/component-library/snippets/ui-elements.ts,
    src/lib/component-library/snippets/cta.ts,
    src/lib/component-library/snippets/media.ts,
    src/lib/component-library/snippets/pricing.ts,
    src/lib/component-library/snippets/notifications.ts,
    src/lib/component-library/types.ts,
    src/lib/component-library/component-library.test.ts,
    .planning/phases/12-migrate-snippet-library-from-daisyui-to-preline-ui/12-RESEARCH.md
  </read_first>
  <action>
Replace the empty arrays in all 6 stub files with full snippet arrays. Each snippet must conform to `ComponentSnippet` interface with all fields.

**COMMON RULES FOR ALL NEW SNIPPETS:**
- `category`: must match the file name (`"forms"`, `"ui-elements"`, `"cta"`, `"media"`, `"pricing"`, `"notifications"`)
- `id`: category-prefixed, unique across entire library (e.g., `"forms-contact"`, `"pricing-three-tier"`)
- `tags`: use only `sections` and `features` vocabulary strings that the analyzer produces (e.g., `"contact-form"`, `"pricing"`, `"cta"`, `"gallery"`, `"newsletter"`, `"alerts"`, `"notifications"`)
- `priority`: 1 (high) to 5 (low) — common patterns get lower (higher priority) numbers
- `domain_hints`: relevant website types (e.g., `["saas", "landing"]` for pricing, `["blog", "portfolio"]` for media)
- `fallback`: `false` for most new snippets. Set `true` ONLY if the snippet is generic enough to serve as a fallback
- `fallback_for`: empty array `[]` unless `fallback: true`
- `min_score`: `0` for most (always eligible), `1` for specialized snippets
- `html`: Pure Tailwind + Preline data-hs-*, dark: prefix everywhere, no CDN links, no DOCTYPE
- All HTML uses placeholder colors (blue-600 primary, gray-900 text, white/gray-50 backgrounds)

**forms.ts** — 8-10 snippets:
1. `forms-contact` — Contact form with name, email, message fields. Tags: `["contact-form", "form"]`. Priority: 1. Domain: `["landing", "portfolio", "business"]`
2. `forms-newsletter` — Newsletter signup with email + submit. Tags: `["newsletter", "form"]`. Priority: 2. Domain: `["blog", "landing"]`
3. `forms-multi-step` — Multi-step registration using Preline `data-hs-stepper`. Tags: `["form", "stepper"]`. Priority: 3. Domain: `["saas", "landing"]`
4. `forms-login` — Login/auth form (email + password). Tags: `["form", "auth"]`. Priority: 3. Domain: `["saas", "dashboard"]`
5. `forms-feedback` — Feedback/rating form with 5-star input (vanilla JS). Tags: `["form", "rating"]`. Priority: 3. Domain: `["saas", "landing"]`
6. `forms-subscribe-inline` — Inline email with validation hint. Tags: `["newsletter", "form"]`. Priority: 2. Domain: `["blog", "landing"]`
7. `forms-file-upload` — Drag-and-drop styled file upload zone. Tags: `["form", "upload"]`. Priority: 4. Domain: `["saas", "dashboard"]`
8. `forms-search` — Search form with icon and Preline dropdown filter. Tags: `["search", "form"]`. Priority: 3. Domain: `["blog", "ecommerce", "saas"]`

**ui-elements.ts** — 8-10 snippets:
1. `ui-elements-alerts` — Alert banners (success/warning/error/info) with Preline `data-hs-remove-element` dismiss. Tags: `["alerts", "notifications"]`. Priority: 2. Domain: `["saas", "dashboard"]`
2. `ui-elements-badges` — Badge collection showcase (color variants). Tags: `["badges"]`. Priority: 3. Domain: `["saas", "ecommerce"]`
3. `ui-elements-breadcrumb` — Breadcrumb navigation. Tags: `["navigation", "breadcrumb"]`. Priority: 3. Domain: `["blog", "ecommerce"]`
4. `ui-elements-pagination` — Pagination bar with prev/next + page numbers. Tags: `["pagination", "navigation"]`. Priority: 3. Domain: `["blog", "ecommerce"]`
5. `ui-elements-progress` — Progress indicators (linear bar + circular). Tags: `["progress"]`. Priority: 3. Domain: `["saas", "dashboard"]`
6. `ui-elements-spinners` — Spinner and loading states (CSS animation). Tags: `["loading"]`. Priority: 4. Domain: `["saas"]`
7. `ui-elements-toggles` — Switch/toggle collection (vanilla JS). Tags: `["toggle", "form"]`. Priority: 4. Domain: `["saas", "dashboard"]`
8. `ui-elements-tooltips` — Tooltip examples. Tags: `["tooltip"]`. Priority: 4. Domain: `["saas", "dashboard"]`

**cta.ts** — 8-10 snippets:
1. `cta-banner-centered` — CTA banner centered with gradient background. Tags: `["cta", "banner"]`. Priority: 1. Domain: `["landing", "saas"]`. Fallback: `true`, fallback_for: `["landing", "generic"]`
2. `cta-email-inline` — CTA with inline email input. Tags: `["cta", "newsletter"]`. Priority: 2. Domain: `["landing", "blog"]`
3. `cta-app-store` — CTA with app store badge placeholders. Tags: `["cta", "download"]`. Priority: 3. Domain: `["landing", "saas"]`
4. `cta-social-proof` — CTA with social proof numbers (users, rating). Tags: `["cta", "social-proof"]`. Priority: 2. Domain: `["landing", "saas"]`
5. `cta-countdown` — CTA with countdown timer (vanilla JS). Tags: `["cta", "timer"]`. Priority: 3. Domain: `["landing", "ecommerce"]`
6. `cta-sticky-bar` — Sticky CTA bar fixed at bottom. Tags: `["cta", "sticky"]`. Priority: 4. Domain: `["landing", "ecommerce"]`
7. `cta-background-image` — CTA with background image overlay. Tags: `["cta", "hero"]`. Priority: 2. Domain: `["landing", "portfolio"]`
8. `cta-split` — CTA split layout (text left, form right). Tags: `["cta", "form"]`. Priority: 2. Domain: `["landing", "saas"]`

**media.ts** — 8-10 snippets:
1. `media-image-grid` — Responsive image grid (CSS grid, 3 cols on lg, 2 on md, 1 on sm). Tags: `["gallery", "images"]`. Priority: 1. Domain: `["portfolio", "blog"]`. Fallback: `true`, fallback_for: `["portfolio"]`
2. `media-image-caption` — Image with caption card. Tags: `["gallery", "images"]`. Priority: 2. Domain: `["portfolio", "blog"]`
3. `media-before-after` — Before/After image slider using range input (vanilla JS). Tags: `["gallery", "comparison"]`. Priority: 3. Domain: `["portfolio", "landing"]`
4. `media-video-embed` — Video embed placeholder (16:9 aspect ratio). Tags: `["video", "media"]`. Priority: 2. Domain: `["landing", "blog"]`
5. `media-gallery-lightbox` — Gallery with Preline modal lightbox (`data-hs-overlay`). Tags: `["gallery", "images"]`. Priority: 2. Domain: `["portfolio"]`
6. `media-audio-player` — Styled HTML5 audio player. Tags: `["audio", "media"]`. Priority: 4. Domain: `["blog", "portfolio"]`
7. `media-map-placeholder` — Map placeholder with overlay CTA. Tags: `["map", "contact-form"]`. Priority: 3. Domain: `["landing", "business"]`
8. `media-logo-cloud` — Logo/partner grid (grayscale image placeholders). Tags: `["logos", "social-proof"]`. Priority: 2. Domain: `["landing", "saas"]`

**pricing.ts** — 6-9 snippets:
1. `pricing-three-tier` — Three-tier pricing table with highlighted middle. Tags: `["pricing"]`. Priority: 1. Domain: `["saas", "landing"]`. Fallback: `true`, fallback_for: `["landing"]`
2. `pricing-toggle` — Monthly/annual toggle (vanilla JS price switch). Tags: `["pricing", "toggle"]`. Priority: 1. Domain: `["saas", "landing"]`
3. `pricing-single-card` — Single highlighted plan card. Tags: `["pricing"]`. Priority: 2. Domain: `["saas"]`
4. `pricing-comparison` — Feature comparison table (checkmark matrix). Tags: `["pricing", "table"]`. Priority: 2. Domain: `["saas"]`
5. `pricing-faq` — Pricing with FAQ accordion (`hs-accordion-group`). Tags: `["pricing", "faq"]`. Priority: 2. Domain: `["saas", "landing"]`
6. `pricing-enterprise` — Enterprise contact CTA pricing card. Tags: `["pricing", "cta"]`. Priority: 3. Domain: `["saas"]`
7. `pricing-freemium` — Freemium vs Pro two-column comparison. Tags: `["pricing"]`. Priority: 2. Domain: `["saas", "landing"]`

**notifications.ts** — 6-9 snippets:
1. `notifications-toast` — Toast notification (top-right corner, Preline `data-hs-remove-element` dismiss). Tags: `["notifications", "alerts"]`. Priority: 2. Domain: `["saas", "dashboard"]`
2. `notifications-alert-strip` — Dismissible alert strip (color variants). Tags: `["alerts", "notifications"]`. Priority: 2. Domain: `["saas", "landing"]`
3. `notifications-dropdown` — Notification dropdown (`hs-dropdown`). Tags: `["notifications", "dropdown"]`. Priority: 3. Domain: `["saas", "dashboard"]`
4. `notifications-cookie-consent` — Cookie consent banner (bottom fixed). Tags: `["cookie", "banner"]`. Priority: 3. Domain: `["landing", "blog"]`
5. `notifications-announcement` — Announcement bar (top of page, dismissible). Tags: `["banner", "notifications"]`. Priority: 2. Domain: `["landing", "ecommerce"]`
6. `notifications-feed` — In-app notification feed list. Tags: `["notifications", "feed"]`. Priority: 3. Domain: `["saas", "dashboard"]`
7. `notifications-empty-state` — Empty state placeholder (icon + message + CTA). Tags: `["empty-state"]`. Priority: 3. Domain: `["saas", "dashboard"]`

**CRITICAL RULES (same as Plan 01):**
- NO CDN links in snippet HTML (test checks for cdn.jsdelivr.net and cdn.tailwindcss.com)
- NO DOCTYPE in snippet HTML
- NO Preline semantic tokens (bg-primary, text-foreground, bg-card)
- NO hs-* variant classes
- ALL snippets include dark: prefix classes
- Use `font-sans` as default font class
- Background palette: `bg-white dark:bg-gray-900` (page), `bg-gray-50 dark:bg-gray-800` or `bg-white dark:bg-gray-800` (cards/sections)
- Buttons: `py-2.5 px-5 inline-flex items-center text-sm font-medium rounded-lg bg-blue-600 text-white hover:bg-blue-700 transition-colors`
- All IDs must be unique across the entire 17-file library

After this task, the combined total across all 17 files should be 100+ snippets.
  </action>
  <verify>
    <automated>npm run test -- component-library && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - forms.ts contains at least 8 snippet objects with IDs starting with `forms-`
    - ui-elements.ts contains at least 8 snippet objects with IDs starting with `ui-elements-`
    - cta.ts contains at least 8 snippet objects with IDs starting with `cta-`
    - media.ts contains at least 8 snippet objects with IDs starting with `media-`
    - pricing.ts contains at least 6 snippet objects with IDs starting with `pricing-`
    - notifications.ts contains at least 6 snippet objects with IDs starting with `notifications-`
    - NONE of the 12 files (6 rewritten + 6 new) contain any DaisyUI patterns (btn-primary, card-body, bg-base-100, etc.)
    - ALL 12 files contain `dark:bg-gray` and `dark:text-`
    - forms.ts contains `data-hs-stepper` (multi-step form)
    - ui-elements.ts contains `data-hs-remove-element` (dismissible alerts)
    - pricing.ts contains `hs-accordion` (FAQ accordion)
    - notifications.ts contains `data-hs-remove-element` (toast dismiss)
    - media.ts contains `data-hs-overlay` (lightbox modal)
    - `npm run test -- component-library` passes ALL tests (including 100+ threshold, DaisyUI detector, dark mode coverage, fallback coverage)
    - `npm run typecheck` passes clean
  </acceptance_criteria>
  <done>All 6 new categories populated with 44-58 new snippets. All 6 remaining existing files rewritten and expanded. Total library: 100+ snippets across 17 categories, zero DaisyUI remnants, all tests green.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npm run test -- component-library` — ALL tests pass:
   - "library has at least 100 snippets" — PASS (100+ total)
   - "no snippet contains DaisyUI class patterns" — PASS (zero remnants)
   - "at least 80% of snippets include dark: prefix" — PASS
   - "all snippet ids are unique" — PASS
   - "every snippet has required fields" — PASS
   - All fallback tests — PASS (fallback metadata preserved)
   - All interactive tests — PASS (quiz, flashcard, timer preserved)
2. `npm run typecheck` — clean
3. Total snippet count across 17 files: 100+
</verification>

<success_criteria>
- ALL_SNIPPETS contains 100+ snippets across 17 categories
- Zero DaisyUI class patterns in any snippet file
- All interactive snippet JS functionality preserved (quiz, flashcard, timer)
- At least 80% of snippets include dark: prefix
- All existing tests pass including DaisyUI remnant detector
- npm run test -- component-library passes clean
- npm run typecheck passes clean
</success_criteria>

<output>
After completion, create `.planning/phases/12-migrate-snippet-library-from-daisyui-to-preline-ui/12-02-SUMMARY.md`
</output>
