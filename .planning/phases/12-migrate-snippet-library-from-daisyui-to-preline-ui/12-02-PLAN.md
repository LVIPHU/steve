---
phase: 12-migrate-snippet-library-from-daisyui-to-preline-ui
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - src/lib/component-library/snippets/stats.ts
  - src/lib/component-library/snippets/testimonials.ts
  - src/lib/component-library/snippets/blog.ts
  - src/lib/component-library/snippets/portfolio.ts
  - src/lib/component-library/snippets/ecommerce.ts
  - src/lib/component-library/snippets/interactive.ts
  - src/lib/component-library/snippets/forms.ts
  - src/lib/component-library/snippets/ui-elements.ts
  - src/lib/component-library/snippets/cta.ts
  - src/lib/component-library/snippets/media.ts
  - src/lib/component-library/snippets/pricing.ts
  - src/lib/component-library/snippets/notifications.ts
  - src/lib/component-library/snippets/index.ts
autonomous: true
requirements: [SNIP-01, SNIP-02, SNIP-03, SNIP-05, SNIP-06, SNIP-07]

must_haves:
  truths:
    - "All 6 remaining snippet files rewritten with zero DaisyUI classes"
    - "6 new category files created with 6-9 snippets each"
    - "snippets/index.ts imports and spreads all 17 category arrays"
    - "ALL_SNIPPETS total count is 100+"
    - "npm run test passes with zero failures"
    - "npm run typecheck passes clean"
  artifacts:
    - path: "src/lib/component-library/snippets/stats.ts"
      provides: "5 stats snippets with Tailwind"
      min_lines: 50
    - path: "src/lib/component-library/snippets/forms.ts"
      provides: "9 form snippets"
      min_lines: 100
    - path: "src/lib/component-library/snippets/ui-elements.ts"
      provides: "8 UI element snippets with Preline behaviors"
      min_lines: 100
    - path: "src/lib/component-library/snippets/cta.ts"
      provides: "8 CTA snippets"
      min_lines: 80
    - path: "src/lib/component-library/snippets/media.ts"
      provides: "8 media snippets"
      min_lines: 80
    - path: "src/lib/component-library/snippets/pricing.ts"
      provides: "6 pricing snippets"
      min_lines: 80
    - path: "src/lib/component-library/snippets/notifications.ts"
      provides: "8 notification snippets"
      min_lines: 80
    - path: "src/lib/component-library/snippets/index.ts"
      provides: "Registry importing all 17 categories"
      contains: "formsSnippets"
  key_links:
    - from: "src/lib/component-library/snippets/index.ts"
      to: "src/lib/component-library/index.ts"
      via: "ALL_SNIPPETS export"
      pattern: "export const ALL_SNIPPETS"
    - from: "src/lib/component-library/snippets/forms.ts"
      to: "src/lib/component-library/snippets/index.ts"
      via: "formsSnippets import"
      pattern: "formsSnippets"
---

<objective>
Rewrite the remaining 6 snippet files from DaisyUI to Tailwind+Preline. Create 6 new category files. Register all 17 categories in index.ts. End state: 100+ snippets, zero DaisyUI remnants, all tests pass.

Purpose: Complete the migration started in Plan 01. After this, the entire snippet library is DaisyUI-free.

Output: 12 rewritten/new snippet files + updated index.ts registry.
</objective>

<execution_context>
@D:/STEVE/steve/.claude/get-shit-done/workflows/execute-plan.md
@D:/STEVE/steve/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-migrate-snippet-library-from-daisyui-to-preline-ui/12-CONTEXT.md
@.planning/phases/12-migrate-snippet-library-from-daisyui-to-preline-ui/12-RESEARCH.md
@.planning/phases/12-migrate-snippet-library-from-daisyui-to-preline-ui/12-01-SUMMARY.md
@.planning/research/PRELINE-UI.md

<interfaces>
From src/lib/component-library/types.ts:
```typescript
export interface ComponentSnippet {
  id: string;
  name: string;
  description: string;
  category: string;
  tags: string[];
  priority: number;
  domain_hints: string[];
  min_score: number;
  fallback: boolean;
  fallback_for: string[];
  html: string;
}
```

From src/lib/component-library/snippets/index.ts (current state with 11 imports):
```typescript
import { heroSnippets } from "./hero";
import { navbarSnippets } from "./navbar";
import { featuresSnippets } from "./features";
import { cardsSnippets } from "./cards";
import { footerSnippets } from "./footer";
import { statsSnippets } from "./stats";
import { testimonialsSnippets } from "./testimonials";
import { interactiveSnippets } from "./interactive";
import { blogSnippets } from "./blog";
import { portfolioSnippets } from "./portfolio";
import { ecommerceSnippets } from "./ecommerce";

export const ALL_SNIPPETS: ComponentSnippet[] = [
  ...heroSnippets, ...navbarSnippets, ...featuresSnippets,
  ...cardsSnippets, ...footerSnippets, ...statsSnippets,
  ...testimonialsSnippets, ...interactiveSnippets, ...blogSnippets,
  ...portfolioSnippets, ...ecommerceSnippets,
];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite 6 remaining snippet files from DaisyUI to Tailwind+Preline</name>
  <files>
    src/lib/component-library/snippets/stats.ts
    src/lib/component-library/snippets/testimonials.ts
    src/lib/component-library/snippets/blog.ts
    src/lib/component-library/snippets/portfolio.ts
    src/lib/component-library/snippets/ecommerce.ts
    src/lib/component-library/snippets/interactive.ts
  </files>
  <read_first>
    - src/lib/component-library/snippets/stats.ts
    - src/lib/component-library/snippets/testimonials.ts
    - src/lib/component-library/snippets/blog.ts
    - src/lib/component-library/snippets/portfolio.ts
    - src/lib/component-library/snippets/ecommerce.ts
    - src/lib/component-library/snippets/interactive.ts
    - src/lib/component-library/types.ts
    - .planning/research/PRELINE-UI.md
    - .planning/phases/12-migrate-snippet-library-from-daisyui-to-preline-ui/12-CONTEXT.md
  </read_first>
  <action>
Rewrite ALL 6 files. For each file: replace ALL DaisyUI classes with Tailwind utilities, add dark: variants everywhere, add 2-3 new snippets. Keep all existing metadata (id, name, tags, priority, fallback, fallback_for, domain_hints, min_score, category) UNCHANGED -- only rewrite `html` content. New snippets get new metadata.

**GLOBAL REPLACEMENT RULES (apply to ALL files):**
- `bg-base-100` -> `bg-white dark:bg-gray-900`
- `bg-base-200` -> `bg-gray-50 dark:bg-gray-900` or `bg-gray-100 dark:bg-gray-800`
- `bg-base-300` -> `bg-gray-200 dark:bg-gray-700`
- `text-base-content` -> `text-gray-900 dark:text-white`
- `text-base-content/60` or `/70` -> `text-gray-600 dark:text-gray-400`
- `text-base-content/40` -> `text-gray-500 dark:text-gray-500`
- `bg-neutral text-neutral-content` -> `bg-gray-900 text-gray-300`
- `btn btn-primary` -> `bg-blue-600 hover:bg-blue-700 text-white px-5 py-2.5 rounded-lg font-medium`
- `btn btn-primary btn-sm` -> `bg-blue-600 hover:bg-blue-700 text-white px-3 py-1.5 rounded-lg text-sm font-medium`
- `btn btn-outline` -> `border border-gray-300 dark:border-gray-600 text-gray-700 dark:text-gray-300 hover:bg-gray-100 dark:hover:bg-gray-800 px-5 py-2.5 rounded-lg font-medium`
- `btn btn-ghost` -> `text-gray-700 dark:text-gray-300 hover:bg-gray-100 dark:hover:bg-gray-800 px-4 py-2 rounded-lg font-medium`
- `btn btn-error` -> `bg-red-600 hover:bg-red-700 text-white px-4 py-2 rounded-lg font-medium`
- `btn btn-success` -> `bg-green-600 hover:bg-green-700 text-white px-4 py-2 rounded-lg font-medium`
- `card bg-base-*` -> `bg-white dark:bg-gray-800 border border-gray-200 dark:border-gray-700 rounded-xl shadow-sm`
- `card-body` -> `p-6`
- `card-title` -> `text-lg font-semibold text-gray-900 dark:text-white`
- `card-actions` -> `flex` with appropriate justify
- `badge badge-primary` -> `inline-flex items-center bg-blue-100 text-blue-800 dark:bg-blue-900 dark:text-blue-300 text-xs font-medium px-2.5 py-0.5 rounded-full`
- `badge badge-outline` -> `inline-flex items-center border border-gray-300 dark:border-gray-600 text-gray-600 dark:text-gray-400 text-xs font-medium px-2.5 py-0.5 rounded-full`
- `badge badge-secondary` -> `inline-flex items-center bg-indigo-100 text-indigo-800 dark:bg-indigo-900 dark:text-indigo-300 text-xs font-medium px-2.5 py-0.5 rounded-full`
- `stat bg-*` -> `bg-white dark:bg-gray-800 border border-gray-200 dark:border-gray-700 rounded-xl p-4`
- `stat-title` -> `text-sm text-gray-500 dark:text-gray-400`
- `stat-value` -> `text-2xl font-bold text-gray-900 dark:text-white`
- `stat-desc` -> `text-sm text-gray-500 dark:text-gray-400`
- `rounded-box` -> `rounded-xl`
- `link link-hover` -> `hover:underline`
- `text-primary` -> `text-blue-600 dark:text-blue-400`
- `from-primary` -> `from-blue-600`
- `to-secondary` -> `to-indigo-600`
- `footer footer-center` -> remove footer-center, use flex/grid
- `mockup-code` -> `bg-gray-900 rounded-xl p-4 font-mono text-sm text-green-400`
- `hsl(var(--b1))` inline styles -> remove, use class `bg-white dark:bg-gray-800`

**STRUCTURAL REWRITES:**

blog.ts timeline: Replace DaisyUI `timeline` with `<div class="relative pl-8 border-l-2 border-gray-200 dark:border-gray-700">` + dot markers using `absolute -left-[25px] w-4 h-4 bg-blue-600 rounded-full`.

interactive.ts `<progress>` elements: Replace with `<div class="w-full bg-gray-200 dark:bg-gray-700 rounded-full h-2"><div class="bg-blue-600 h-2 rounded-full" style="width: 75%"></div></div>`.

interactive.ts JS class mutations: Replace `classList.add('btn-success')` with `classList.add('bg-green-600', 'text-white')`. Replace `classList.add('btn-error')` with `classList.add('bg-red-600', 'text-white')`. Replace `classList.remove('btn-primary')` with `classList.remove('bg-blue-600')`. Replace `el.className = 'btn btn-sm ...'` with equivalent Tailwind utility strings.

interactive.ts flashcard: Remove `style="background: hsl(var(--b1))"`, use `class="bg-white dark:bg-gray-800"`.

interactive.ts mockup-code: Replace with `<div class="bg-gray-900 rounded-xl p-4 overflow-x-auto"><code class="font-mono text-sm text-green-400">...</code></div>`.

**PRESERVE in interactive.ts:** ALL JS functionality (setInterval, setTimeout, localStorage with appgen- prefix, touch/mouse events, CSS 3D flip animations with perspective/backface-visibility/transform-style, quiz scoring). Only change CSS class references.

### New snippets per file:

**stats.ts** (3 existing + 2 new):
- `stats-with-icon-grid`: category="stats", tags=["stats"], priority=2, fallback=false. 4-stat grid with colored icon circles + number + label.
- `stats-live-counter`: category="stats", tags=["stats", "counter"], priority=2, domain_hints=["dashboard"], fallback=false. Inline script animates numbers 0-to-target via requestAnimationFrame. Each stat uses `data-target="1240"`.

**testimonials.ts** (3 existing + 2 new):
- `testimonial-carousel`: category="testimonials", tags=["testimonials"], priority=2, fallback=false. One testimonial visible, prev/next arrows, vanilla JS slides.
- `testimonial-company-logos`: category="testimonials", tags=["testimonials"], priority=2, domain_hints=["landing"], fallback=false. Testimonials + "Trusted by" logo bar with placeholder gray boxes.

**blog.ts** (5 existing + 2 new):
- `blog-featured-post`: category="blog", tags=["blog", "article-grid"], priority=2, domain_hints=["blog"], fallback=false. Full-width featured card + 2-col grid below.
- `blog-newsletter-signup`: category="blog", tags=["blog", "newsletter"], priority=2, domain_hints=["blog"], fallback=false. Email input + subscribe in a colored card.

**portfolio.ts** (5 existing + 2 new):
- `portfolio-masonry-grid`: category="portfolio", tags=["gallery", "portfolio"], priority=2, domain_hints=["portfolio"], fallback=false. CSS `columns-1 md:columns-2 lg:columns-3` masonry.
- `portfolio-case-study`: category="portfolio", tags=["portfolio"], priority=2, domain_hints=["portfolio"], fallback=false. Structured case study: challenge/solution/results.

**ecommerce.ts** (4 existing + 2 new):
- `product-card-grid`: category="ecommerce", tags=["ecommerce", "cards"], priority=2, fallback=false. 4-col product grid with stars + Add to Cart.
- `price-toggle-annual`: category="ecommerce", tags=["ecommerce", "pricing"], priority=2, fallback=false. Monthly/annual toggle with inline script.

**interactive.ts** (6 existing + 3 new):
- `before-after-slider`: category="interactive", tags=["interactive", "gallery"], priority=2, fallback=false. Draggable slider comparing two overlapping divs. Vanilla JS mouse/touch.
- `typing-animation-hero`: category="interactive", tags=["hero", "interactive"], priority=2, domain_hints=["landing"], fallback=false. Cycles typed strings with blinking cursor CSS.
- `count-up-on-scroll`: category="interactive", tags=["stats", "interactive", "counter"], priority=2, fallback=false. IntersectionObserver triggers requestAnimationFrame counter. Uses `data-target`.
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - stats.ts contains ZERO occurrences of `bg-base-` or `stat-title` or `stat-value` or `stat-desc` or `rounded-box`
    - stats.ts contains `stats-with-icon-grid` and `stats-live-counter` snippet ids
    - stats.ts contains `data-target` (live counter attribute)
    - testimonials.ts contains ZERO occurrences of `bg-base-` or `card-body` or `badge badge-`
    - testimonials.ts contains `testimonial-carousel` and `testimonial-company-logos` snippet ids
    - blog.ts contains ZERO occurrences of `bg-base-` or `timeline` or `card-body` or `badge badge-` or `btn btn-`
    - blog.ts contains `blog-featured-post` and `blog-newsletter-signup` snippet ids
    - blog.ts contains `border-l-2` (timeline replacement pattern)
    - portfolio.ts contains ZERO occurrences of `bg-base-` or `card-body` or `badge badge-` or `btn btn-`
    - portfolio.ts contains `portfolio-masonry-grid` and `portfolio-case-study` snippet ids
    - portfolio.ts contains `columns-` (masonry CSS)
    - ecommerce.ts contains ZERO occurrences of `bg-base-` or `card-body` or `btn btn-`
    - ecommerce.ts contains `product-card-grid` and `price-toggle-annual` snippet ids
    - interactive.ts contains ZERO occurrences of `bg-base-` or `btn-primary` or `btn-success` or `btn-error` or `mockup-code` or `hsl(var(`
    - interactive.ts contains `before-after-slider` and `typing-animation-hero` and `count-up-on-scroll` snippet ids
    - interactive.ts contains `perspective: 1000px` (flashcard preserved)
    - interactive.ts contains `appgen-` (localStorage prefix preserved)
    - interactive.ts contains `touchstart` (touch events preserved)
    - `npx tsc --noEmit` passes clean
  </acceptance_criteria>
  <done>All 6 remaining snippet files fully rewritten with Tailwind utilities, zero DaisyUI classes, dark mode on every snippet, and 2-3 new snippets per file. All existing JS functionality in interactive.ts preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Create 6 new category files and register in index.ts</name>
  <files>
    src/lib/component-library/snippets/forms.ts
    src/lib/component-library/snippets/ui-elements.ts
    src/lib/component-library/snippets/cta.ts
    src/lib/component-library/snippets/media.ts
    src/lib/component-library/snippets/pricing.ts
    src/lib/component-library/snippets/notifications.ts
    src/lib/component-library/snippets/index.ts
  </files>
  <read_first>
    - src/lib/component-library/snippets/index.ts
    - src/lib/component-library/snippets/hero.ts (reference for snippet structure after Plan 01 rewrite)
    - src/lib/component-library/types.ts
    - .planning/research/PRELINE-UI.md
    - .planning/phases/12-migrate-snippet-library-from-daisyui-to-preline-ui/12-CONTEXT.md
  </read_first>
  <action>
Create 6 NEW files and update index.ts to register them. Every snippet uses ONLY Tailwind utility classes + Preline data-hs-* attributes. NO DaisyUI classes. ALL snippets include dark: variants.

**Color rule:** Use Tailwind palette as placeholder (blue-600 for primary, gray-* for neutrals). NO semantic tokens.

**Dark mode rule:** Every snippet: page bg `bg-white dark:bg-gray-900`, cards `bg-white dark:bg-gray-800`, text `text-gray-900 dark:text-white` / `text-gray-600 dark:text-gray-400`, borders `border-gray-200 dark:border-gray-700`.

### forms.ts (9 snippets, export as `formsSnippets`)

Each snippet: `import type { ComponentSnippet } from "../types";`

1. `contact-form`: category="forms", tags=["forms", "cta"], priority=1, domain_hints=["landing"], min_score=1, fallback=true, fallback_for=["landing"]. Full-width section with name, email, message fields + submit button. Inputs: `bg-white dark:bg-gray-800 border border-gray-300 dark:border-gray-600 rounded-lg px-4 py-2.5 text-gray-900 dark:text-white placeholder-gray-400 focus:ring-2 focus:ring-blue-500 focus:border-blue-500`. Submit: `bg-blue-600 hover:bg-blue-700 text-white px-6 py-2.5 rounded-lg font-medium w-full`.

2. `newsletter-signup`: category="forms", tags=["forms", "newsletter"], priority=1, min_score=1, fallback=false. Inline email + button row. Input: same as above. Button: `bg-blue-600 hover:bg-blue-700 text-white px-6 py-2.5 rounded-lg font-medium`.

3. `login-form`: category="forms", tags=["forms"], priority=2, min_score=1, fallback=false. Centered card with email + password + remember me checkbox + submit + "Forgot password?" link.

4. `register-form`: category="forms", tags=["forms"], priority=2, min_score=1, fallback=false. Card with name, email, password, confirm password fields + terms checkbox + submit.

5. `search-bar`: category="forms", tags=["forms", "search"], priority=2, min_score=1, fallback=false. Search input with magnifying glass SVG icon inside, rounded-full. `bg-gray-100 dark:bg-gray-800 border-0 rounded-full pl-10 pr-4 py-2.5`.

6. `filter-sidebar`: category="forms", tags=["forms", "filters"], priority=2, min_score=1, fallback=false. Vertical sidebar with checkbox groups for categories + price range + "Apply Filters" button.

7. `multi-step-form`: category="forms", tags=["forms", "stepper"], priority=2, min_score=1, fallback=false. 3-step form with step indicator at top. Vanilla JS shows/hides steps. Step indicator: numbered circles connected by lines, active step highlighted blue.

8. `file-upload`: category="forms", tags=["forms"], priority=2, min_score=1, fallback=false. Drag-and-drop area with dashed border, cloud upload icon, "Click or drag files" text. `border-2 border-dashed border-gray-300 dark:border-gray-600 rounded-xl p-8 text-center`.

9. `rating-input`: category="forms", tags=["forms", "interactive"], priority=2, min_score=1, fallback=false. Star rating (5 stars) with hover + click using vanilla JS. Stars: SVG with fill toggling between `text-yellow-400` (filled) and `text-gray-300 dark:text-gray-600` (empty).

### ui-elements.ts (8 snippets, export as `uiElementsSnippets`)

1. `accordion-faq`: category="ui-elements", tags=["accordion", "faq"], priority=1, domain_hints=["landing"], min_score=1, fallback=true, fallback_for=["landing", "generic"]. Preline accordion: `<div class="hs-accordion-group">` with 4-5 FAQ items. Each: `<div class="hs-accordion" id="faq-N">`, toggle button with `class="hs-accordion-toggle"`, content with `class="hs-accordion-content w-full overflow-hidden transition-[height] duration-300"`. Plus/minus SVGs toggle via `hs-accordion-active:hidden` / `hs-accordion-active:block`.

2. `tabs-content`: category="ui-elements", tags=["tabs"], priority=1, min_score=1, fallback=false. Preline tabs: tab nav with `data-hs-tab="#panel-1"` on each tab link. Tab buttons use `hs-tab-active:bg-blue-600 hs-tab-active:text-white bg-gray-100 dark:bg-gray-800 text-gray-600 dark:text-gray-400 px-4 py-2 rounded-lg`. Panels with `id="panel-1" role="tabpanel"`.

3. `modal-confirm`: category="ui-elements", tags=["modal"], priority=2, min_score=1, fallback=false. Preline overlay modal: trigger button with `data-hs-overlay="#confirm-modal"`. Modal: `<div id="confirm-modal" class="hs-overlay hidden size-full fixed top-0 start-0 z-[80] overflow-x-hidden overflow-y-auto">` with backdrop `bg-black/50` and centered card with title, message, Cancel + Confirm buttons.

4. `dropdown-menu`: category="ui-elements", tags=["dropdown"], priority=2, min_score=1, fallback=false. Preline dropdown: `<div class="hs-dropdown relative inline-flex">` with trigger button `data-hs-dropdown-toggle`, menu `class="hs-dropdown-menu hidden hs-dropdown-open:opacity-100 min-w-60 bg-white dark:bg-gray-800 shadow-lg rounded-lg p-2 mt-2 border border-gray-200 dark:border-gray-700"`. 4-5 menu items with hover states.

5. `tooltip-card`: category="ui-elements", tags=["tooltip"], priority=2, min_score=1, fallback=false. Card with hover tooltip. Use CSS-only tooltip: `group relative` wrapper, tooltip div `invisible group-hover:visible opacity-0 group-hover:opacity-100 transition-opacity bg-gray-900 text-white text-sm rounded-lg py-2 px-3 absolute`.

6. `badge-group`: category="ui-elements", tags=["badges"], priority=2, min_score=1, fallback=false. Collection of badge styles: solid `bg-blue-600 text-white`, soft `bg-blue-100 text-blue-800 dark:bg-blue-900 dark:text-blue-300`, outline `border border-blue-600 text-blue-600`, with dot indicator, dismissible with `data-hs-remove-element`.

7. `progress-steps`: category="ui-elements", tags=["stepper", "progress"], priority=2, min_score=1, fallback=false. Horizontal step indicator with numbered circles connected by lines. Completed: `bg-blue-600 text-white`. Current: `border-2 border-blue-600 text-blue-600`. Upcoming: `bg-gray-200 dark:bg-gray-700 text-gray-500`.

8. `notification-toast`: category="ui-elements", tags=["notification", "toast"], priority=2, min_score=1, fallback=false. Fixed-position toast notification (bottom-right). Slide-in animation via CSS keyframes. Dismiss button. `bg-white dark:bg-gray-800 border border-gray-200 dark:border-gray-700 rounded-xl shadow-lg p-4`. Auto-dismiss with setTimeout in inline script.

### cta.ts (8 snippets, export as `ctaSnippets`)

1. `cta-centered`: category="cta", tags=["cta"], priority=1, domain_hints=["landing"], min_score=1, fallback=true, fallback_for=["landing", "generic"]. Centered headline + subtitle + 2 buttons on `bg-blue-600` background. Text: `text-white`. Buttons: white primary `bg-white text-blue-600 hover:bg-gray-100`, outline `border border-white text-white hover:bg-white/10`.

2. `cta-split`: category="cta", tags=["cta"], priority=2, min_score=1, fallback=false. Two-column: text left, illustration/pattern right. `bg-gray-50 dark:bg-gray-800 rounded-2xl`.

3. `cta-with-image`: category="cta", tags=["cta"], priority=2, min_score=1, fallback=false. CTA overlaying a gradient background image placeholder. Text centered over gradient overlay.

4. `cta-email-capture`: category="cta", tags=["cta", "newsletter", "forms"], priority=1, min_score=1, fallback=false. CTA with inline email input + subscribe button. `bg-gradient-to-r from-blue-600 to-indigo-600 text-white rounded-2xl p-8`.

5. `cta-app-download`: category="cta", tags=["cta"], priority=2, min_score=1, fallback=false. App download CTA with placeholder store badges (styled divs for App Store + Google Play). Dark background.

6. `cta-video-play`: category="cta", tags=["cta", "video"], priority=2, min_score=1, fallback=false. CTA with play button circle in center. Click opens a Preline overlay modal with video embed placeholder.

7. `cta-countdown-timer`: category="cta", tags=["cta", "timer", "interactive"], priority=2, min_score=1, fallback=false. Urgency CTA with JS countdown timer showing days:hours:minutes:seconds. Inline script uses setInterval.

8. `cta-social-proof`: category="cta", tags=["cta", "testimonials"], priority=2, min_score=1, fallback=false. CTA section with avatar stack + "Join 10,000+ users" + star rating + action button.

### media.ts (8 snippets, export as `mediaSnippets`)

1. `image-gallery-grid`: category="media", tags=["gallery"], priority=1, domain_hints=["portfolio"], min_score=1, fallback=true, fallback_for=["portfolio"]. 3-column responsive grid of image placeholders (gradient divs). Hover overlay with title. `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4`.

2. `image-lightbox`: category="media", tags=["gallery", "modal"], priority=2, min_score=1, fallback=false. Gallery grid where clicking an image opens it in a Preline overlay modal. Trigger: `data-hs-overlay="#lightbox-modal"`. Modal shows enlarged image + close button.

3. `video-embed`: category="media", tags=["video"], priority=2, min_score=1, fallback=false. Responsive video embed (16:9 aspect ratio). `<div class="relative w-full" style="padding-bottom: 56.25%"><iframe class="absolute inset-0 w-full h-full rounded-xl" src="about:blank" title="Video"></iframe></div>`. Placeholder with play icon overlay.

4. `video-hero-bg`: category="media", tags=["hero", "video"], priority=2, domain_hints=["landing"], min_score=1, fallback=false. Full-screen hero with video/gradient background, dark overlay, centered text. Similar to hero-video-bg but in media category for tag diversity.

5. `carousel-images`: category="media", tags=["gallery", "interactive"], priority=2, min_score=1, fallback=false. Image carousel with prev/next arrows and dot indicators. Vanilla JS for slide transitions with CSS transform. 3-5 placeholder slides.

6. `before-after-media`: category="media", tags=["gallery", "interactive"], priority=2, min_score=1, fallback=false. Before/after comparison with a vertical slider handle. Two overlapping divs (gradient placeholders), draggable divider. Inline script handles mouse/touch.

7. `audio-player`: category="media", tags=["audio", "interactive"], priority=2, min_score=1, fallback=false. Styled audio player card with play/pause button, progress bar (div-based), track title, time display. Vanilla JS controls. `bg-white dark:bg-gray-800 rounded-xl p-4 shadow-sm`.

8. `map-embed`: category="media", tags=["map", "contact"], priority=2, domain_hints=["landing"], min_score=1, fallback=false. Responsive map placeholder (gray box with pin icon) in a card with address + contact info on the side.

### pricing.ts (6 snippets, export as `pricingSnippets`)

1. `pricing-2-col`: category="pricing", tags=["pricing"], priority=1, domain_hints=["landing"], min_score=1, fallback=true, fallback_for=["landing"]. Two pricing cards side by side. Featured card has `border-2 border-blue-600` + "Popular" badge. Price: `text-4xl font-bold`. Feature list with check icons. CTA button per card.

2. `pricing-3-col`: category="pricing", tags=["pricing"], priority=1, min_score=1, fallback=false. Three pricing tiers. Middle card elevated with `shadow-xl` + blue border. `grid grid-cols-1 md:grid-cols-3 gap-8`.

3. `pricing-comparison-table`: category="pricing", tags=["pricing", "table"], priority=2, min_score=1, fallback=false. Full comparison table with features as rows and plans as columns. Check/X marks per cell. `<table class="w-full">` with proper th/td styling.

4. `pricing-toggle-monthly-annual`: category="pricing", tags=["pricing", "interactive"], priority=1, min_score=1, fallback=false. Toggle switch between monthly/annual pricing. Vanilla JS: toggle button switches displayed prices. Annual shows savings badge. Toggle styled with `bg-gray-200 dark:bg-gray-700 rounded-full p-1` container + sliding `bg-blue-600 rounded-full` indicator.

5. `pricing-enterprise-contact`: category="pricing", tags=["pricing", "cta"], priority=2, min_score=1, fallback=false. Enterprise tier card with "Contact Us" CTA instead of price. Lists enterprise features. `bg-gray-900 text-white rounded-2xl`.

6. `pricing-feature-list`: category="pricing", tags=["pricing"], priority=2, min_score=1, fallback=false. Single pricing card with extensive feature list. Each feature: green check SVG + feature text. Grouped by category with dividers.

### notifications.ts (8 snippets, export as `notificationsSnippets`)

1. `alert-info`: category="notifications", tags=["alert", "notification"], priority=1, min_score=1, fallback=true, fallback_for=["generic", "dashboard"]. Info alert: `bg-blue-50 dark:bg-blue-900/20 border border-blue-200 dark:border-blue-800 text-blue-800 dark:text-blue-400 rounded-lg p-4`. Info SVG icon + text.

2. `alert-success`: category="notifications", tags=["alert"], priority=2, min_score=1, fallback=false. Success alert: `bg-green-50 dark:bg-green-900/20 border border-green-200 dark:border-green-800 text-green-800 dark:text-green-400`.

3. `alert-warning`: category="notifications", tags=["alert"], priority=2, min_score=1, fallback=false. Warning alert: `bg-yellow-50 dark:bg-yellow-900/20 border border-yellow-200 dark:border-yellow-800 text-yellow-800 dark:text-yellow-400`.

4. `alert-error`: category="notifications", tags=["alert"], priority=2, min_score=1, fallback=false. Error alert: `bg-red-50 dark:bg-red-900/20 border border-red-200 dark:border-red-800 text-red-800 dark:text-red-400`.

5. `dismissible-banner`: category="notifications", tags=["alert", "notification"], priority=2, min_score=1, fallback=false. Full-width banner at top with message + dismiss X button. Use Preline `data-hs-remove-element="#banner-id"` for dismiss. `bg-blue-600 text-white py-3 px-4`.

6. `notification-badge`: category="notifications", tags=["notification", "badges"], priority=2, min_score=1, fallback=false. Bell icon button with red notification dot (animated ping). Click opens Preline dropdown with notification list. `data-hs-dropdown` on wrapper.

7. `toast-stack`: category="notifications", tags=["notification", "toast"], priority=2, min_score=1, fallback=false. Stack of 3 toast notifications in bottom-right corner. Each has icon + text + close button. Staggered entry via CSS animation delays. Inline script auto-dismisses after 5s.

8. `inline-form-validation`: category="notifications", tags=["forms", "alert"], priority=2, min_score=1, fallback=false. Form with inline validation messages: error state `border-red-500 focus:ring-red-500` on input + red text below, success state `border-green-500` + green check icon.

### Update index.ts

Add 6 new imports and spread them into ALL_SNIPPETS:

```typescript
import type { ComponentSnippet } from "../types";
import { heroSnippets } from "./hero";
import { navbarSnippets } from "./navbar";
import { featuresSnippets } from "./features";
import { cardsSnippets } from "./cards";
import { footerSnippets } from "./footer";
import { statsSnippets } from "./stats";
import { testimonialsSnippets } from "./testimonials";
import { interactiveSnippets } from "./interactive";
import { blogSnippets } from "./blog";
import { portfolioSnippets } from "./portfolio";
import { ecommerceSnippets } from "./ecommerce";
import { formsSnippets } from "./forms";
import { uiElementsSnippets } from "./ui-elements";
import { ctaSnippets } from "./cta";
import { mediaSnippets } from "./media";
import { pricingSnippets } from "./pricing";
import { notificationsSnippets } from "./notifications";

export const ALL_SNIPPETS: ComponentSnippet[] = [
  ...heroSnippets,
  ...navbarSnippets,
  ...featuresSnippets,
  ...cardsSnippets,
  ...footerSnippets,
  ...statsSnippets,
  ...testimonialsSnippets,
  ...interactiveSnippets,
  ...blogSnippets,
  ...portfolioSnippets,
  ...ecommerceSnippets,
  ...formsSnippets,
  ...uiElementsSnippets,
  ...ctaSnippets,
  ...mediaSnippets,
  ...pricingSnippets,
  ...notificationsSnippets,
];
```
  </action>
  <verify>
    <automated>npm run test 2>&1 | tail -20; echo "---TYPECHECK---"; npm run typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - forms.ts exists and exports `formsSnippets` with 9 snippets
    - forms.ts contains `contact-form` and `multi-step-form` and `rating-input` snippet ids
    - ui-elements.ts exists and exports `uiElementsSnippets` with 8 snippets
    - ui-elements.ts contains `hs-accordion-group` (Preline accordion)
    - ui-elements.ts contains `data-hs-tab` (Preline tabs)
    - ui-elements.ts contains `data-hs-overlay` (Preline modal)
    - ui-elements.ts contains `data-hs-dropdown` (Preline dropdown)
    - ui-elements.ts contains `data-hs-remove-element` (Preline dismiss)
    - cta.ts exists and exports `ctaSnippets` with 8 snippets
    - cta.ts contains `cta-centered` and `cta-countdown-timer` snippet ids
    - media.ts exists and exports `mediaSnippets` with 8 snippets
    - media.ts contains `image-gallery-grid` and `carousel-images` snippet ids
    - pricing.ts exists and exports `pricingSnippets` with 6 snippets
    - pricing.ts contains `pricing-toggle-monthly-annual` snippet id
    - notifications.ts exists and exports `notificationsSnippets` with 8 snippets
    - notifications.ts contains `data-hs-remove-element` (dismissible banner)
    - notifications.ts contains `data-hs-dropdown` (notification badge dropdown)
    - index.ts imports all 17 category arrays (formsSnippets, uiElementsSnippets, ctaSnippets, mediaSnippets, pricingSnippets, notificationsSnippets)
    - index.ts spreads all 17 arrays into ALL_SNIPPETS
    - NONE of the 6 new files contain any DaisyUI classes (bg-base-, btn btn-, card-body, etc.)
    - ALL 6 new files contain `dark:bg-gray-` (dark mode support)
    - `npm run test` passes with zero failures (including threshold >= 100 and DaisyUI remnant detector)
    - `npm run typecheck` passes clean
  </acceptance_criteria>
  <done>6 new category files created (forms, ui-elements, cta, media, pricing, notifications) with 47 total new snippets. All registered in index.ts. Combined with the 11 existing categories (now expanded), ALL_SNIPPETS contains 100+ snippets. All tests pass including DaisyUI remnant detector. TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `npm run test` -- ALL tests pass including snippet count >= 100 and DaisyUI remnant detector
2. `npm run typecheck` -- clean pass
3. `grep -r "bg-base-\|btn btn-\|card-body\|stat-title\|navbar-start\|footer-center\|mockup-code\|badge badge-\|daisyui" src/lib/component-library/snippets/` -- must return empty
4. Count total snippets: `node -e "const {ALL_SNIPPETS} = require('./src/lib/component-library/snippets'); console.log(ALL_SNIPPETS.length)"` or equivalent -- must be >= 100
</verification>

<success_criteria>
- All 17 snippet files exist with zero DaisyUI classes
- ALL_SNIPPETS length is 100+
- All 57+ tests pass (original tests + new DaisyUI remnant detector)
- TypeScript compiles clean
- Every snippet has dark: prefix classes
- Preline data-hs-* patterns used in ui-elements.ts (accordion, tabs, modal, dropdown, dismiss)
- All interactive snippet JS functionality preserved
</success_criteria>

<output>
After completion, create `.planning/phases/12-migrate-snippet-library-from-daisyui-to-preline-ui/12-02-SUMMARY.md`
</output>
